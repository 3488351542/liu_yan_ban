# 部署上线教学

> 目标：把你的留言板发到网上，任何人都能通过网址访问

---

## 整体流程

```
你的电脑                  →  本地开发（别人看不到）
    ↓  git push
GitHub                    →  存代码的中转站
    ↓  部署平台拉取
云服务器                  →  24 小时在线运行
    ↓
用户访问 https://你的网址   →  留言板
```

---

## 第 1 步：注册 Zeabur

> Zeabur = 部署平台（zi bo 兹伯）
> 中国公司，国内访问快，有免费额度

1. 打开 https://zeabur.com
2. 点 **Sign up**（sign up = 注册，sai na pu 赛那普）
3. 用 GitHub 账号登录（点 "Continue with GitHub"）
4. 授权后自动登录成功

---

## 第 2 步：创建项目

登录后：

1. 点 **Create Project**（create = 创建，ke rui ei te 克瑞埃特）
   （project = 项目，pu ruo jie ke te 普若杰克特）
2. 选 **Deploy from GitHub**（deploy = 部署，di pu luo yi 迪普洛伊）
3. 在列表里找到 `liu_yan_ban`，点 **Import**（import = 导入，yin pao te 因泡特）

---

## 第 3 步：等待自动部署

Zeabur 会自动检测到这是 Python Flask 项目，自动运行。

你会看到一些日志（log = 日志，lao ge 涝格）在滚动：

```
Installing dependencies   安装依赖（flask）
Starting server           启动服务器
Running on port 8080      运行在 8080 端口
```

等出现 `Running` 或 `Active` 状态就行。

---

## 第 4 步：绑定域名

默认 Zeabur 会给你一个临时网址，比如 `liu-yan-ban.zeabur.app`。

如果你有自己的域名（如 `liuyanban.com`），可以绑定。没有的话直接用默认的就行。

---

## 第 5 步：修改代码后如何更新

以后你改了代码：

```
git add .
git commit -m "改了xxx"
git push
```

Zeabur 检测到 GitHub 有更新，**自动重新部署**，你什么都不用做。

---

## 常见问题

### Q: 部署后 database.db 在哪？

在本地电脑上。部署后服务器上**没有**你本地的留言数据。
如果需要保留数据，以后会学连接远程数据库（阶段二的内容）。

### Q: 免费额度够用吗？

Zeabur 免费版够你学习用，流量小的话不用花钱。

### Q: 国内访问慢吗？

Zeabur 是国内公司，服务器在香港，国内访问速度不错。
