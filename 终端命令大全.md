# 终端命令大全（PowerShell + CMD）

> 重点：每个参数标注 **固定**（不能改）还是 **自定义**（你自己填的）
> 全部用小写

### 例子中出现的单词发音

| 英文 | 拼音 | 中文 |
|------|------|------|
| echo | yi ke ou 伊科欧 | 输出 |
| test | tai si te 泰斯特 | 测试 |
| my_project | mai pu ruo jie ke te 迈普若杰克特 | 我的项目 |
| a.txt | a tai ke si te 埃泰科斯特 | 文件A |
| b.txt | bi tai ke si te 比泰科斯特 | 文件B |
| backup | bai ke a pu 百克阿普 | 备份 |
| ping | pin 聘 | 测试连通 |
| path | pa si 帕斯 | 系统路径 |
| verbose | ve bou si 沃伯斯 | 详细输出 |

---

## 一、目录操作（在哪、去哪、看什么）

### 1.1 dir — 查看当前目录有哪些文件

```powershell
dir
dir 文件夹路径
dir *.py
```

| 部分 | 固定/自定义 | 翻译 | 拼音 |
|------|-----------|------|------|
| `dir` | ✅ **固定** | directory = 目录 | di er 迪尔 |
| `*.py` | ❌ **自定义** | py 结尾的文件 | |
| `*` | ✅ **固定** | 任意字符 | |

---

### 1.2 cd — 切换目录

```powershell
cd 文件夹名
cd ..
cd /d d:\project
```

| 部分 | 固定/自定义 | 翻译 | 拼音 |
|------|-----------|------|------|
| `cd` | ✅ **固定** | change directory = 切换目录 | xi di 西迪 |
| `..` | ✅ **固定** | 上一级目录 | |
| `/d` | ✅ **固定** | 切换盘符 | |

---

### 1.3 mkdir — 创建文件夹

```powershell
mkdir 文件夹名
mkdir a\b\c
```

| 部分 | 固定/自定义 | 翻译 | 拼音 |
|------|-----------|------|------|
| `mkdir` | ✅ **固定** | make directory = 创建目录 | mai ke di er 迈克迪尔 |

---

### 1.4 pwd / chdir — 查看当前路径

```powershell
pwd
chdir
```

| 部分 | 翻译 | 拼音 |
|------|------|------|
| `pwd` | print working directory = 打印当前路径 | pi wa bu li di 皮瓦不理迪 |
| `chdir` | change directory = 切换目录 | xi di 西迪 |

---

## 二、文件操作（增删改）

### 2.1 echo — 输出内容

```powershell
echo 内容
echo "你好"
```

| 部分 | 固定/自定义 | 翻译 | 拼音 |
|------|-----------|------|------|
| `echo` | ✅ **固定** | 回显 / 输出 | yi ke ou 伊科欧 |

---

### 2.2 type / cat — 查看文件内容

```powershell
type 文件名
cat 文件名
```

| 部分 | 翻译 | 拼音 |
|------|------|------|
| `type` | 查看文件内容 | tai pu 太普 |
| `cat` | concatenate = 连接查看 | kai te 开特 |

---

### 2.3 copy / cp — 复制文件

```powershell
copy 来源文件 目标文件
cp 来源文件 目标位置
```

| 部分 | 翻译 | 拼音 |
|------|------|------|
| `copy` | 复制 | kao pi 考批 |
| `cp` | copy 简写 | |

---

### 2.4 move / mv — 移动或重命名

```powershell
move 来源 目标
mv 旧名 新名
```

| 部分 | 翻译 | 拼音 |
|------|------|------|
| `move` | 移动 | mu fu 姆夫 |
| `mv` | move 简写 | |

---

### 2.5 del / rm — 删除文件

```powershell
del 文件名
rm 文件名
remove-item 文件名
```

| 部分 | 翻译 | 拼音 |
|------|------|------|
| `del` | delete = 删除 | di er 迪尔 |
| `rm` | remove = 移除 | a er em 阿尔埃姆 |
| `remove-item` | 移除项目 | rui mu fu yi te mu 瑞姆夫伊特姆 |

---

## 三、系统操作

### 3.1 cls / clear — 清屏

```powershell
cls
clear
```

| 部分 | 翻译 | 拼音 |
|------|------|------|
| `cls` | clear screen = 清屏 | xi li er si ke rui en 西利尔斯科瑞恩 |
| `clear` | 清除 | ke li er 克利尔 |

---

### 3.2 ver — 查看系统版本

```powershell
ver
```

| 部分 | 翻译 | 拼音 |
|------|------|------|
| `ver` | version = 版本 | ve 沃 |

---

### 3.3 exit — 退出终端

```powershell
exit
```

| 部分 | 翻译 | 拼音 |
|------|------|------|
| `exit` | 退出 | yi ge si te 伊格斯特 |

---

## 四、网络命令

### 4.1 ping — 测试网络连通

```powershell
ping 域名或IP
ping -n 5 baidu.com
```

| 部分 | 固定/自定义 | 翻译 | 拼音 |
|------|-----------|------|------|
| `ping` | ✅ **固定** | 测试连通 | pin 聘 |
| `-n` | ✅ **固定** | count = 发送次数 | |

---

### 4.2 ipconfig — 查看本机 IP

```powershell
ipconfig
ipconfig /all
```

| 部分 | 固定/自定义 | 翻译 | 拼音 |
|------|-----------|------|------|
| `ipconfig` | ✅ **固定** | IP configuration = IP 配置 | ai pi ken fi ge 埃皮肯菲格 |
| `/all` | ✅ **固定** | all = 全部信息 | |

---

## 五、PowerShell 特殊符号

### 5.1 `$` — 变量

```powershell
$name = "张三"
```

| 符号 | 固定/自定义 | 翻译 | 拼音 |
|------|-----------|------|------|
| `$` | ✅ **固定** | 变量标记 | dollar 道了 |
| `=` | ✅ **固定** | 赋值 | |

---

### 5.2 `$env:` — 环境变量

```powershell
$env:path
$env:userprofile
```

| 符号 | 翻译 | 拼音 |
|------|------|------|
| `$env` | environment = 环境 | yin wai en men te 因外恩门特 |
| `path` | 系统路径 | pa si 帕斯 |
| `userprofile` | 用户目录 | yo ze pu rou fai er 优则普肉法伊尔 |
| `computername` | 电脑名 | kem piu te nei mu 克姆皮优特内姆 |

---

### 5.3 `@()` — 数组

```powershell
$list = @(1, 2, 3)
```

### 5.4 `%` — 遍历

```powershell
1..5 | % { echo $_ }
```

| 符号 | 翻译 |
|------|------|
| `%` | foreach = 遍历 |
| `$_` | 当前对象 |

---

## 六、CMD 批处理特殊符号

### 6.1 `@` — 不显示命令本身

```cmd
@echo off
```

| 部分 | 翻译 | 拼音 |
|------|------|------|
| `@` | 不显示这一行 | ai te 埃特 |
| `off` | 关闭 | ao fu 奥夫 |

---

### 6.2 `>` — 输出到文件（覆盖）

```cmd
echo hello > a.txt
```

| 符号 | 翻译 |
|------|------|
| `>` | 重定向（写入文件） |

---

### 6.3 `>>` — 追加到文件

```cmd
echo hello >> a.txt
```

---

### 6.4 `|` — 管道

```cmd
dir | find "py"
```

| 部分 | 翻译 | 拼音 |
|------|------|------|
| `\|` | 管道 | pai pu 派普 |
| `find` | 搜索 | fai en de 法因得 |

---

### 6.5 `%` — CMD 变量

```cmd
set name=张三
echo %name%
```

| 部分 | 翻译 | 拼音 |
|------|------|------|
| `set` | 设置 | sai te 赛特 |
| `%name%` | 引用变量 | |

---

## 七、最常用的 15 个命令速查

| 序号 | 命令 | 翻译 | 拼音 | 做什么 |
|------|------|------|------|--------|
| 1 | `dir` | 目录 | di er 迪尔 | 看当前文件夹的文件 |
| 2 | `cd 文件夹` | 切换目录 | xi di 西迪 | 进入文件夹 |
| 3 | `cd ..` | 上一级 | - | 回到上级 |
| 4 | `mkdir 名` | 创建目录 | mai ke di er 迈克迪尔 | 新建文件夹 |
| 5 | `type 文件` | 查看 | tai pu 太普 | 看文件内容 |
| 6 | `echo 内容` | 输出 | yi ke ou 伊科欧 | 打印内容 |
| 7 | `copy a b` | 复制 | kao pi 考批 | 复制文件 |
| 8 | `move a b` | 移动 | mu fu 姆夫 | 移动文件 |
| 9 | `del 文件` | 删除 | di er 迪尔 | 删文件 |
| 10 | `cls` | 清屏 | xi li er 西利尔 | 清空屏幕 |
| 11 | `ping 域名` | 测试网络 | pin 聘 | 看通不通 |
| 12 | `ipconfig` | IP配置 | ai pi ken fi ge 埃皮肯菲格 | 看本机IP |
| 13 | `exit` | 退出 | yi ge si te 伊格斯特 | 关掉终端 |
| 14 | `.` | 当前目录 | - | 代表当前文件夹 |
| 15 | `..` | 上级目录 | - | 代表上一级文件夹 |
