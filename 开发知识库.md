# 开发知识库

> 记录 Web 开发中遇到的问题、解决方案、技术栈、专业术语
> 给自己看的，方便以后开发别的网站用

---

## 一、常见问题与解决方案

### 1. 历史栈污染

| 项目 | 内容 |
|------|------|
| **现象** | 点返回按钮，不是回退到上一页，而是跳转到一个新页面，历史记录越来越多 |
| **原因** | 用了 `location.href = '/xxx'` 做返回，产生新历史记录 |
| **解决** | 用 `javascript:history.back()` 或 `history.back()` 回退 |
| **代码** | `<a href="javascript:history.back()">←</a>` |

### 2. 客户端状态丢失

| 项目 | 内容 |
|------|------|
| **现象** | 页面跳转后，搜索结果、滚动位置、Tab 状态全没了 |
| **原因** | JS 变量在内存里，页面刷新就清空 |
| **解决** | 用 `sessionStorage` 保存状态，`pageshow` 事件恢复 |
| **代码** | `sessionStorage.setItem("key", value)` / `sessionStorage.getItem("key")` |

### 3. bfcache 不兼容

| 项目 | 内容 |
|------|------|
| **现象** | 从详情页返回，页面没有自动恢复搜索弹窗 |
| **原因** | 浏览器有页面缓存（bfcache），`window.addEventListener('load')` 不触发 |
| **解决** | 用 `pageshow` 事件替代 `load` |
| **代码** | `window.addEventListener('pageshow', function(e){ ... })` |

### 4. 状态残留

| 项目 | 内容 |
|------|------|
| **现象** | 关闭搜索弹窗后，下次打开页面又自动弹出来 |
| **原因** | `sessionStorage` 没清空，`pageshow` 时检测到数据又打开了 |
| **解决** | 关闭时清空 `sessionStorage` |
| **代码** | `sessionStorage.removeItem("key")` |

### 5. 图片加载慢

| 项目 | 内容 |
|------|------|
| **原因** | Flask/Gunicorn 返回图片慢，没有缓存，图片没压缩 |
| **解决** | 上传时压缩（Pillow）+ 浏览器缓存头 + CDN |
| **代码** | `Cache-Control: public, max-age=86400` |

### 6. 时区问题

| 项目 | 内容 |
|------|------|
| **现象** | 服务器在美国（UTC），数据库存的时间比中国慢8小时 |
| **原因** | `CURRENT_TIMESTAMP` 用的是服务器本地时间 |
| **解决** | 用 Python 生成北京时间再存数据库 |
| **代码** | `datetime.utcnow() + timedelta(hours=8)` |

### 7. 图片 404（部署后丢失）

| 项目 | 内容 |
|------|------|
| **现象** | 本地能看到的图片，部署到服务器就 404 |
| **原因** | 本地图片没进 Git，或者 Railway 重启容器清空了文件 |
| **解决** | 用云存储（Cloudflare R2 / 阿里云 OSS）存图片 |

---

## 二、优化技术

### 1. 缓存优化

| 技术 | 说明 | 代码 |
|------|------|------|
| **浏览器缓存** | 第二次访问同一图片不用下载 | `Cache-Control: max-age=86400` |
| **Redis 缓存** | 把数据库查询结果存内存，快 10 倍 | `@cached(30)` 装饰器 |
| **CDN 缓存** | 全国/全球节点缓存，用户就近访问 | Cloudflare / 阿里云 CDN |

### 2. 数据库优化

| 技术 | 说明 | 代码 |
|------|------|------|
| **索引** | 给经常查的列建目录，查得快 | `create index on 表名(列名)` |
| **连接池** | 提前建好数据库连接，高并发复用 | `ThreadedConnectionPool(2, 10)` |
| **分页** | 一次只取一页，不全部加载 | `limit 20 offset 0` |

### 3. 图片优化

| 技术 | 说明 |
|------|------|
| **压缩** | 上传时 PNG 原样保留/其他转 JPEG 质量80%，最长边1200px |
| **缩略图** | 列表用小图，详情用大图 |
| **WebP** | 比 JPEG 小 30%，质量不变 |
| **图床（OSS）** | 图片存阿里云 OSS，Railway 重启不丢 |
| **OSS 自动切换** | 有 OSS 配置走 OSS，没有就走本地 |

---

## 三、专业术语表

| 术语 | 中文 | 解释 |
|------|------|------|
| History Stack Pollution | 历史栈污染 | 返回按钮产生了新历史记录，不是回退 |
| Client-side State Loss | 客户端状态丢失 | 页面跳转导致 JS 变量清空 |
| bfcache | 返回/前进缓存 | 浏览器缓存整个页面，返回时不重新加载 |
| State Leak | 状态残留 | 关闭操作没清空持久化数据，下次又出现 |
| Cache Stampede | 缓存雪崩 | 大量缓存同时过期，所有请求打到数据库 |
| Connection Pool | 连接池 | 提前建好数据库连接，高并发复用 |
| Connection Leak | 连接泄漏 | 数据库连接用完没关闭，耗尽服务器连接数 |
| Pool Exhaustion | 连接池耗尽 | 连接池最大连接数不够用 |
| Sequence Desync | 序列不同步 | 手动插入数据后自增ID不连续 |
| Worker Timeout | 工作进程超时 | 请求处理太久被 Gunicorn 断开 |
| Port Mismatch | 端口不匹配 | 容器内外端口号不一致 |
| Test Drift | 测试漂移 | 代码改了但测试没更新 |
| Timezone Drift | 时区漂移 | 服务器时区不同导致时间显示错误 |
| Index Scan | 索引扫描 | 通过索引快速定位数据 |
| Full Table Scan | 全表扫描 | 没有索引时从头翻到尾查数据 |
| B+ Tree | B+树 | 索引使用的数据结构，能快速查找 |
| TTL | 过期时间 | Redis 缓存自动删除的时间 |
| Fallback | 降级 | Redis 连不上时自动走数据库，不崩溃 |
| Object Storage | 对象存储 | 云存储服务，存图片/文件，不占服务器磁盘 |
| CDN | 内容分发网络 | 全球节点缓存文件，用户就近访问加速 |
| Connection Pool | 连接池 | 提前建好数据库连接，高并发复用 |
| OSS | 阿里云对象存储 | 阿里云的云存储服务，国内访问快 |
| AccessKey | 访问密钥 | 阿里云 API 的钥匙，分 ID 和 Secret |
| RAM | 访问控制 | 阿里云的权限管理，可以开子账号 |
| Endpoint | 接入点 | OSS 的服务器地址，比如 oss-cn-hangzhou.aliyuncs.com |
| Bucket | 存储桶 | OSS 里存文件的容器，类似文件夹 |
| Master-Slave | 主从复制 | 主库写数据，从库读数据，读写分离 |
| Read-Write Split | 读写分离 | 写操作走主库，读操作走从库，提高并发 |
| QPS | 每秒查询数 | 衡量系统并发能力的指标 |
| Pod | 容器实例 | Kubernetes 的最小部署单元 |
| CDN | 内容分发网络 | 静态资源缓存到离用户最近的节点 |
| Rate Limiting | 限流 | 控制每用户每秒允许的请求次数 |
| Microservices | 微服务 | 把单体应用拆成多个独立服务 |
| Kubernetes | 容器编排 | 自动管理容器的扩缩容和部署 |
| Message Queue | 消息队列 | 异步处理任务，削峰填谷 |
| Celery | 异步任务队列 | Python 后台任务框架，发帖/压缩图片异步处理 |
| Async | 异步 | 请求立即返回，后台慢慢处理 |
| Sync | 同步 | 请求一直等到处理完才返回 |
| Worker | 工作进程 | Celery 的后台小弟，专门处理耗时任务 |
| Broker | 中间人 | Celery 的消息队列（Redis），收发任务 |
| Cold Start | 冷启动 | 服务长时间无请求后需重新唤醒，第一次慢 |
| Fallback | 降级 | 异步不可用时自动转为同步处理 |

---

## 四、技术栈速查

### Web 框架

| 技术 | 用途 | 说明 |
|------|------|------|
| **Flask** | Python Web 框架 | 轻量级，单文件可以开发 |
| **Jinja2** | 模板引擎 | Flask 内置，`{% for %}` 循环渲染 HTML |
| **Gunicorn** | WSGI 服务器 | 生产环境运行 Flask，支持高并发 |

### 数据库

| 技术 | 用途 | 说明 |
|------|------|------|
| **PostgreSQL** | 关系数据库 | 开源，功能强大 |
| **psycopg2** | Python 连接 PostgreSQL | `pip install psycopg2-binary` |
| **Redis** | 内存缓存 | 缓存数据库查询结果，快 10 倍 |
| **连接池** | 复用数据库连接 | `ThreadedConnectionPool(2, 10)` |

### 云存储

| 技术 | 用途 | 说明 |
|------|------|------|
| **阿里云 OSS** | 图片存储 | 免费 5GB，国内访问快 |
| **oss2** | Python OSS SDK | `pip install oss2` |
| **Cloudflare R2** | 图片存储替代方案 | 免费 10GB，需绑卡 |

### 前端

| 技术 | 用途 | 说明 |
|------|------|------|
| **HTML + CSS + JS** | 前端三件套 | 没用框架，纯原生 |
| **fetch API** | 发 AJAX 请求 | 原生 JS，不需要 axios |
| **sessionStorage** | 浏览器端存储 | 页面关闭自动清空 |
| **Responsive** | 响应式设计 | `@media (max-width: 600px)` |

### 部署

| 技术 | 用途 | 说明 |
|------|------|------|
| **Railway** | 云部署平台 | 自动从 GitHub 部署 |
| **Docker** | 容器化 | 打包应用和依赖 |
| **Cloudflare** | CDN + DNS | 加速静态资源 |
| **Gunicorn** | Python 生产服务器 | `gunicorn app:app` |

---

## 五、性能指标参考

| 指标 | 好 | 一般 | 差 |
|------|-----|-----|-----|
| 首页加载 | < 1秒 | 1-3秒 | > 3秒 |
| 数据库查询 | < 10ms | 10-50ms | > 100ms |
| 图片加载 | < 200KB | 200KB-1MB | > 1MB |
| 并发人数 | 有连接池 | 无连接池 | 无连接池+无缓存 |
| 日活支撑 | 有 Redis 缓存 | 有数据库索引 | 啥都没有 |
