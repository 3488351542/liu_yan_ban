# 从零开发微信小程序完整实施文档

> 参考"承德应职院微墙"Web 版项目经验，从零搭建微信小程序版
> 写给 AI 阅读的完整技术方案，包含每一步的具体参数、代码、配置

---

## 一、架构设计

### Web 版架构（参考）

```
用户浏览器 → Flask（Python）→ PostgreSQL → 本地文件/OSS
             ├── Jinja2 模板渲染
             ├── Session 登录
             ├── AJAX（fetch API）
             └── Gunicorn 部署
```

### 小程序架构（目标）

```
微信客户端 → 云函数（Node.js）→ 云数据库（MongoDB 兼容）
             ├── WXML/WXSS 渲染
             ├── 微信登录（openid）
             ├── wx.request API
             └── 微信开发者工具上传
```

### 需实现的文件清单

| 模块 | 小程序实现 | 参考 Web 版 |
|------|-----------|------------|
| 后端路由 | 14 个云函数 | `app/routes.py`（500行） |
| 数据库操作 | 云数据库 SDK | `app/models.py`（420行） |
| 工具函数 | 云函数公共模块 | `app/utils.py`（80行） |
| 缓存 | 不需要（云数据库自带） | `app/cache.py`（45行） |
| 异步任务 | 云函数异步处理 | `app/tasks.py`（50行） |
| 搜索 | 云数据库正则搜索 | `app/search.py`（80行） |
| 前端页面 | 8 个 WXML 页面 | `templates/*.html`（12个） |

---

## 二、项目目录结构

```
miniprogram/
├── project.config.json          # 小程序项目配置
├── miniprogram/                 # 小程序代码
│   ├── app.js                   # 全局逻辑
│   ├── app.json                 # 全局配置（页面路由、窗口样式）
│   ├── app.wxss                 # 全局样式
│   ├── sitemap.json             # 搜索配置
│   ├── pages/                   # 页面目录
│   │   ├── index/               # 首页（帖子列表）
│   │   │   ├── index.wxml
│   │   │   ├── index.wxss
│   │   │   ├── index.js
│   │   │   └── index.json
│   │   ├── post-detail/         # 帖子详情页
│   │   ├── publish/             # 发布页
│   │   ├── my/                  # 我的页面
│   │   ├── hot/                 # 今日热榜
│   │   ├── comments/            # 历史评论
│   │   ├── search/              # 搜索页
│   │   └── login/               # 登录页
│   ├── components/              # 公共组件
│   │   ├── post-card/           # 帖子卡片组件
│   │   └── avatar/              # 头像组件
│   ├── images/                  # 本地图片
│   └── utils/                   # 工具函数
│       ├── api.js               # API 封装（wx.request）
│       ├── util.js              # 时间格式化等
│       └── auth.js              # 微信登录
├── cloudfunctions/              # 云函数
│   ├── login/                   # 微信登录
│   │   ├── index.js
│   │   └── package.json
│   ├── getPosts/                # 获取帖子列表
│   ├── getPostDetail/           # 帖子详情
│   ├── createPost/              # 发帖
│   ├── toggleLike/              # 点赞切换
│   ├── toggleFav/               # 收藏切换
│   ├── reply/                   # 回复
│   ├── search/                  # 搜索
│   ├── getHot/                  # 热榜
│   ├── getUserPosts/            # 我的帖子
│   ├── getUserFavs/             # 我的收藏
│   ├── getUserStats/            # 用户统计
│   ├── uploadImage/             # 图片上传
│   └── deletePost/              # 删除帖子
├── cloudbaserc.json             # CloudBase 配置
└── package.json                 # 依赖
```

---

## 三、数据库设计（云数据库）

### 3.1 集合设计

云数据库是文档型（类似 MongoDB），不需要建表，直接插入文档。

#### `posts` 集合（原 messages 表）

```json
{
  "_id": "自动生成",
  "username": "邮箱前缀或昵称",
  "content": "帖子内容",
  "reply_to": null,           // null=顶级帖子, 有值=回复
  "user_email": "微信 openid",
  "image_url": "云存储文件ID",
  "category": "message",      // message/daily/used
  "likes_count": 0,
  "comments_count": 0,
  "liked_by": ["openid1", "openid2"],  // 点赞的用户列表（替代 likes 表）
  "faved_by": ["openid1"],             // 收藏的用户列表（替代 favorites 表）
  "author_avatar": "云存储文件ID",
  "author_nickname": "用户昵称",
  "created_at": "2026-06-09T12:00:00Z"
}
```

#### `users` 集合（原 users 表）

```json
{
  "_id": "自动生成",
  "openid": "微信唯一标识",
  "nickname": "用户昵称",
  "avatar_url": "微信头像URL",
  "phone": "",
  "role": "user",           // user / admin
  "created_at": "2026-06-09T12:00:00Z"
}
```

#### `chat_messages` 集合（不变，保留）

### 3.2 集合创建（CloudBase MCP 执行）

```bash
# AI 通过 CloudBase MCP 创建集合和索引：
cloudbase.database.createCollection("posts")
cloudbase.database.createCollection("users")
cloudbase.database.createCollection("chat_messages")
# 创建索引
cloudbase.database.createIndex("posts", {category: 1, created_at: -1})
cloudbase.database.createIndex("posts", {reply_to: 1})
cloudbase.database.createIndex("posts", {user_email: 1})
cloudbase.database.createIndex("posts", {content: "text"})  # 全文搜索索引
```

### 3.3 数据库操作封装（云函数公共模块）

```javascript
// cloudfunctions/common/db.js
const cloud = require('wx-server-sdk')
cloud.init()
const db = cloud.database()
const _ = db.command

// === posts 操作 ===
exports.getPosts = async (category, page = 1, perPage = 20, openid = null) => {
  const offset = (page - 1) * perPage
  let query = { reply_to: null }
  if (category && category !== 'latest') query.category = category
  
  const result = await db.collection('posts')
    .where(query)
    .orderBy('created_at', 'desc')
    .skip(offset)
    .limit(perPage)
    .get()
  
  return result.data.map(p => ({
    ...p,
    liked: openid ? (p.liked_by || []).includes(openid) : false,
    favorited: openid ? (p.faved_by || []).includes(openid) : false,
    replies_list: [],
    replies_count: p.comments_count || 0
  }))
}

exports.toggleLike = async (postId, openid) => {
  const post = await db.collection('posts').doc(postId).get()
  const liked_by = post.data.liked_by || []
  const hasLiked = liked_by.includes(openid)
  
  if (hasLiked) {
    await db.collection('posts').doc(postId).update({
      data: { liked_by: _.pull(openid), likes_count: _.inc(-1) }
    })
    return { liked: false, count: Math.max(0, (post.data.likes_count || 0) - 1) }
  } else {
    await db.collection('posts').doc(postId).update({
      data: { liked_by: _.push(openid), likes_count: _.inc(1) }
    })
    return { liked: true, count: (post.data.likes_count || 0) + 1 }
  }
}

exports.toggleFav = async (postId, openid) => {
  const post = await db.collection('posts').doc(postId).get()
  const faved_by = post.data.faved_by || []
  const hasFaved = faved_by.includes(openid)
  
  if (hasFaved) {
    await db.collection('posts').doc(postId).update({
      data: { faved_by: _.pull(openid) }
    })
    return { favorited: false }
  } else {
    await db.collection('posts').doc(postId).update({
      data: { faved_by: _.push(openid) }
    })
    return { favorited: true }
  }
}
```

---

## 四、云函数详细实现

### 4.1 微信登录（cloudfunctions/login/index.js）

```javascript
const cloud = require('wx-server-sdk')
cloud.init()
const db = cloud.database()

// 云函数入口
exports.main = async (event, context) => {
  const { OPENID } = cloud.getWXContext()
  
  // 查是否已有用户
  const user = await db.collection('users').where({ openid: OPENID }).get()
  
  if (user.data.length === 0) {
    // 新用户自动注册
    await db.collection('users').add({
      data: {
        openid: OPENID,
        nickname: `用户${OPENID.slice(-4)}`,
        avatar_url: '',
        role: OPENID === 'o123456789' ? 'admin' : 'user',  // 管理员 openid 写死
        created_at: new Date()
      }
    })
    return { isNew: true, openid: OPENID }
  }
  
  return { isNew: false, openid: OPENID, user: user.data[0] }
}
```

### 4.2 获取帖子列表（cloudfunctions/getPosts/index.js）

```javascript
const cloud = require('wx-server-sdk')
cloud.init()
const db = cloud.database()
const _ = db.command

exports.main = async (event) => {
  const { category = 'latest', page = 1, perPage = 20, openid = '' } = event
  const offset = (page - 1) * perPage
  
  let query = { reply_to: null }
  if (category && category !== 'latest') query.category = category
  
  try {
    const result = await db.collection('posts')
      .where(query)
      .orderBy('created_at', 'desc')
      .skip(offset)
      .limit(perPage)
      .get()
    
    const posts = result.data.map(p => ({
      _id: p._id,
      username: p.author_nickname || p.username,
      content: p.content,
      image_url: p.image_url,
      category: p.category || 'message',
      author_avatar: p.author_avatar,
      likes_count: p.likes_count || 0,
      comments_count: p.comments_count || 0,
      replies_count: p.comments_count || 0,
      liked: openid ? (p.liked_by || []).includes(openid) : false,
      favorited: openid ? (p.faved_by || []).includes(openid) : false,
      created_at: p.created_at
    }))
    
    return { code: 0, data: posts }
  } catch (err) {
    return { code: -1, error: err.message }
  }
}
```

### 4.3 搜索（cloudfunctions/search/index.js）

```javascript
const cloud = require('wx-server-sdk')
cloud.init()
const db = cloud.database()
const _ = db.command

exports.main = async (event) => {
  const { keyword = '', page = 1, perPage = 20, openid = '' } = event
  if (!keyword.trim()) return { code: 0, data: [] }
  
  try {
    // 云数据库的全文搜索（需要创建 text 索引）
    const result = await db.collection('posts')
      .where(_.and([
        { reply_to: null },
        db.command.or([
          { content: db.RegExp({ regexp: keyword, options: 'i' }) }
        ])
      ]))
      .orderBy('created_at', 'desc')
      .skip((page - 1) * perPage)
      .limit(perPage)
      .get()
    
    return { code: 0, data: result.data }
  } catch (err) {
    return { code: -1, error: err.message }
  }
}
```

### 4.4 图片上传（cloudfunctions/uploadImage/index.js）

```javascript
const cloud = require('wx-server-sdk')
cloud.init()

exports.main = async (event) => {
  const { filePath } = event  // 微信临时文件路径
  
  try {
    const result = await cloud.uploadFile({
      cloudPath: `uploads/${Date.now()}_${Math.random().toString(36).slice(-6)}.jpg`,
      filePath: filePath,
    })
    
    // 获取云存储文件 URL
    const url = await cloud.getTempFileURL({
      fileList: [result.fileID]
    })
    
    return { code: 0, url: url.fileList[0].tempFileURL, fileID: result.fileID }
  } catch (err) {
    return { code: -1, error: err.message }
  }
}
```

---

## 五、小程序页面路由

### 5.1 app.json 配置

```json
{
  "pages": [
    "pages/index/index",
    "pages/post-detail/post-detail",
    "pages/publish/publish",
    "pages/my/my",
    "pages/hot/hot",
    "pages/comments/comments",
    "pages/search/search",
    "pages/login/login"
  ],
  "tabBar": {
    "color": "#999",
    "selectedColor": "#667eea",
    "backgroundColor": "#fff",
    "borderStyle": "black",
    "list": [
      {
        "pagePath": "pages/index/index",
        "iconPath": "images/home.png",
        "selectedIconPath": "images/home-active.png",
        "text": "首页"
      },
      {
        "pagePath": "pages/publish/publish",
        "iconPath": "images/add.png",
        "selectedIconPath": "images/add-active.png",
        "text": "发布"
      },
      {
        "pagePath": "pages/my/my",
        "iconPath": "images/my.png",
        "selectedIconPath": "images/my-active.png",
        "text": "我的"
      }
    ]
  },
  "window": {
    "navigationBarBackgroundColor": "#667eea",
    "navigationBarTitleText": "承德应职院微墙",
    "navigationBarTextStyle": "white"
  }
}
```

### 5.2 页面路由对照表

| Web 路由 | 小程序页面 | 传参 |
|---------|-----------|------|
| `/` | `pages/index/index` | `?category=latest` |
| `/post/<id>` | `pages/post-detail/post-detail` | `?id=xxx` |
| `/my` | `pages/my/my` | — |
| `/my?tab=posts` | `pages/my/my?tab=posts` | — |
| `/my?tab=favorites` | `pages/my/my?tab=favorites` | — |
| `/my/comments` | `pages/comments/comments` | — |
| `/hot` | `pages/hot/hot` | — |
| `/login` | `pages/login/login` | — |
| 搜索弹窗 | `pages/search/search` | `?q=关键词` |

---

## 六、WXML 页面示例

### 6.1 首页（pages/index/index.wxml）

```xml
<!-- 顶部标题 -->
<view class="header">
  <text class="title">承德应职院微墙</text>
  <text class="subtitle">分享校园生活，记录点滴美好</text>
</view>

<!-- 搜索框 -->
<view class="search-bar" bindtap="goSearch">
  <input disabled placeholder="🔍 搜索消息..." />
</view>

<!-- 版权信息 -->
<view class="info-row">
  <text class="copyright">著作者:谢先生</text>
  <text class="wechat">微信号:xjx200504091731</text>
</view>

<!-- 热榜入口 -->
<view class="hot-bar" bindtap="goHot">
  <text class="fire">🔥</text>
  <text class="hot-label">今日热榜</text>
  <text>点击查看最火帖子 ›</text>
</view>

<!-- Tab 切换 -->
<view class="tab-bar">
  <view class="tab-item {{currentCategory=='latest'?'active':''}}" 
        bindtap="switchTab" data-category="latest">最新</view>
  <view class="tab-item {{currentCategory=='message'?'active':''}}" 
        bindtap="switchTab" data-category="message">留言板</view>
  <view class="tab-item {{currentCategory=='daily'?'active':''}}" 
        bindtap="switchTab" data-category="daily">日常投稿</view>
  <view class="tab-item {{currentCategory=='used'?'active':''}}" 
        bindtap="switchTab" data-category="used">二手闲置</view>
</view>

<!-- 帖子列表 -->
<scroll-view scroll-y class="post-list" bindscrolltolower="loadMore">
  <view class="post-card" 
        wx:for="{{posts}}" wx:key="_id"
        bindtap="goDetail" data-id="{{item._id}}">
    <!-- 头部：头像+用户名+时间+分类 -->
    <view class="post-header">
      <view class="post-avatar">
        <image wx:if="{{item.author_avatar}}" src="{{item.author_avatar}}"/>
        <text wx:else>{{item.username.charAt(0)}}</text>
      </view>
      <view class="post-info">
        <text class="post-username">{{item.username}}</text>
        <view class="post-meta">
          <text>{{item.created_at}}</text>
          <text class="cat-{{item.category}}">
            {{item.category=='message'?'留言板':item.category=='daily'?'日常投稿':'二手闲置'}}
          </text>
        </view>
      </view>
    </view>
    
    <!-- 内容 -->
    <text class="post-content">{{item.content}}</text>
    
    <!-- 图片 -->
    <image wx:if="{{item.image_url}}" src="{{item.image_url}}" 
           class="post-image" mode="widthFix" bindtap="previewImage"/>
    
    <!-- 操作栏 -->
    <view class="post-actions">
      <view class="action-btn {{item.liked?'liked':''}}" 
            bindtap="toggleLike" data-id="{{item._id}}">
        <text>{{item.liked?'❤️':'🤍'}}</text>
        <text>{{item.likes_count}}</text>
      </view>
      <view class="action-btn {{item.favorited?'favorited':''}}" 
            bindtap="toggleFav" data-id="{{item._id}}">
        <text>{{item.favorited?'⭐':'☆'}}</text>
      </view>
      <view class="action-btn" bindtap="goDetail" data-id="{{item._id}}">
        <text>💬 {{item.replies_count}}</text>
      </view>
    </view>
  </view>
  
  <!-- 空状态 -->
  <view class="empty-state" wx:if="{{posts.length==0}}">
    <text class="empty-icon">📭</text>
    <text>还没有内容，快来发布第一条吧！</text>
  </view>
</scroll-view>
```

### 6.2 首页 JavaScript（pages/index/index.js）

```javascript
const api = require('../../utils/api')

Page({
  data: {
    posts: [],
    currentCategory: 'latest',
    page: 1,
    loading: false,
    hasMore: true
  },

  onLoad(options) {
    if (options.category) {
      this.setData({ currentCategory: options.category })
    }
    this.loadPosts(true)
    this.checkLogin()
  },

  // 检查微信登录
  async checkLogin() {
    const app = getApp()
    if (!app.globalData.openid) {
      wx.showToast({ title: '正在登录...', icon: 'none' })
      await app.doLogin()
    }
  },

  // 加载帖子
  async loadPosts(reset = false) {
    if (this.data.loading) return
    this.setData({ loading: true })
    
    if (reset) {
      this.setData({ page: 1, hasMore: true })
    }
    
    try {
      const res = await api.call('getPosts', {
        category: this.data.currentCategory,
        page: this.data.page,
        limit: 20,
        openid: getApp().globalData.openid || ''
      })
      
      if (res.code === 0) {
        this.setData({
          posts: reset ? res.data : [...this.data.posts, ...res.data],
          page: this.data.page + 1,
          hasMore: res.data.length === 20
        })
      }
    } catch (err) {
      wx.showToast({ title: '加载失败', icon: 'none' })
    } finally {
      this.setData({ loading: false })
    }
  },

  // 下拉刷新
  onPullDownRefresh() {
    this.loadPosts(true).then(() => wx.stopPullDownRefresh())
  },

  // 上拉加载更多
  loadMore() {
    if (this.data.hasMore) this.loadPosts()
  },

  // 切换Tab
  switchTab(e) {
    const category = e.currentTarget.dataset.category
    this.setData({ currentCategory: category })
    this.loadPosts(true)
  },

  // 搜索
  goSearch() {
    wx.navigateTo({ url: '/pages/search/search' })
  },

  // 热榜
  goHot() {
    wx.navigateTo({ url: '/pages/hot/hot' })
  },

  // 详情页
  goDetail(e) {
    const id = e.currentTarget.dataset.id
    wx.navigateTo({ url: `/pages/post-detail/post-detail?id=${id}` })
  },

  // 点赞
  async toggleLike(e) {
    const id = e.currentTarget.dataset.id
    const res = await api.call('toggleLike', { postId: id, openid: getApp().globalData.openid })
    if (res.code === 0) {
      // 更新本地状态
      const posts = this.data.posts.map(p => {
        if (p._id === id) {
          p.liked = res.data.liked
          p.likes_count = res.data.count
        }
        return p
      })
      this.setData({ posts })
    }
  },

  // 收藏
  async toggleFav(e) {
    const id = e.currentTarget.dataset.id
    const res = await api.call('toggleFav', { postId: id, openid: getApp().globalData.openid })
    if (res.code === 0) {
      const posts = this.data.posts.map(p => {
        if (p._id === id) {
          p.favorited = res.data.favorited
        }
        return p
      })
      this.setData({ posts })
      wx.showToast({ title: res.data.favorited ? '✅ 已收藏' : '已取消收藏' })
    }
  }
})
```

---

## 七、API 封装（utils/api.js）

```javascript
/**
 * 云函数调用封装
 * 所有后端操作通过云函数实现
 */
const call = (name, data = {}) => {
  return new Promise((resolve, reject) => {
    wx.cloud.callFunction({
      name,
      data,
      success: res => resolve(res.result),
      fail: err => reject(err)
    })
  })
}

// 各API封装
module.exports = {
  call,

  // 帖子
  getPosts: (category, page, openid) => 
    call('getPosts', { category, page, openid }),
  
  getPostDetail: (id, openid) => 
    call('getPostDetail', { postId: id, openid }),
  
  createPost: (data) => 
    call('createPost', data),
  
  deletePost: (id) => 
    call('deletePost', { postId: id }),

  // 互动
  toggleLike: (postId, openid) => 
    call('toggleLike', { postId, openid }),
  
  toggleFav: (postId, openid) => 
    call('toggleFav', { postId, openid }),
  
  reply: (postId, content, openid) => 
    call('reply', { postId, content, openid }),

  // 搜索
  search: (keyword, page, openid) => 
    call('search', { keyword, page, openid }),

  // 热榜
  getHot: (openid) => 
    call('getHot', { openid }),

  // 个人中心
  getUserPosts: (openid, page) => 
    call('getUserPosts', { openid, page }),
  
  getUserFavs: (openid, page) => 
    call('getUserFavs', { openid, page }),
  
  getUserStats: (openid) => 
    call('getUserStats', { openid }),
  
  getUserComments: (openid, page) => 
    call('getUserComments', { openid, page }),

  // 用户
  updateProfile: (openid, data) => 
    call('updateProfile', { openid, ...data }),

  // 图片
  uploadImage: (filePath) => 
    call('uploadImage', { filePath })
}
```

---

## 八、微信登录流程（app.js）

```javascript
// app.js
App({
  globalData: {
    openid: null,
    userInfo: null
  },

  onLaunch() {
    // 初始化云环境
    wx.cloud.init({
      env: '你的云环境ID',  // 从 CloudBase 控制台获取
      traceUser: true
    })
    
    // 自动登录
    this.doLogin()
  },

  async doLogin() {
    try {
      // 1. 微信登录获取 code
      const { code } = await wx.login()
      
      // 2. 调用云函数登录
      const res = await wx.cloud.callFunction({
        name: 'login',
        data: { code }
      })
      
      this.globalData.openid = res.result.openid
      
      // 3. 存储用户信息
      wx.setStorageSync('openid', res.result.openid)
      
      return res.result
    } catch (err) {
      console.error('登录失败', err)
      return null
    }
  },

  // 检查管理员
  isAdmin() {
    const adminOpenids = ['o123456789']  // 从原 ADMIN_EMAIL 映射
    return adminOpenids.includes(this.globalData.openid)
  }
})
```

---

## 九、云环境配置

### 9.1 cloudbaserc.json

```json
{
  "envId": "你的云环境ID",
  "functionRoot": "cloudfunctions",
  "functions": [
    {
      "name": "login",
      "timeout": 5,
      "memory": 128
    },
    {
      "name": "getPosts",
      "timeout": 10,
      "memory": 256
    },
    {
      "name": "createPost",
      "timeout": 10,
      "memory": 256
    },
    {
      "name": "toggleLike",
      "timeout": 5,
      "memory": 128
    },
    {
      "name": "toggleFav",
      "timeout": 5,
      "memory": 128
    },
    {
      "name": "search",
      "timeout": 10,
      "memory": 256
    },
    {
      "name": "uploadImage",
      "timeout": 30,
      "memory": 512
    },
    {
      "name": "getHot",
      "timeout": 10,
      "memory": 256
    },
    {
      "name": "deletePost",
      "timeout": 5,
      "memory": 128
    }
  ]
}
```

### 9.2 project.config.json

```json
{
  "description": "承德应职院微墙",
  "packOptions": { "ignore": [] },
  "setting": {
    "bundle": false,
    "userConfirmedBundleSwitch": false,
    "urlCheck": true,
    "scopeDataCheck": false,
    "coverView": true,
    "es6": true,
    "postcss": true,
    "compileHotReLoad": false,
    "lazyloadPlaceholderEnable": false,
    "preloadBackgroundData": false,
    "minified": true,
    "autoAudits": false,
    "newFeature": false,
    "uglifyFileName": false,
    "uploadWithSourceMap": true,
    "useIsolateContext": true,
    "nodeModules": false,
    "enhance": true,
    "useMultiFrameRuntime": true,
    "useApiHook": true,
    "useApiHostProcess": true,
    "showShadowRootInWxmlPanel": true,
    "packNpmManually": false,
    "enableEngp": false,
    "packNpmRelationList": [],
    "minifyWXSS": true,
    "showES6CompileOption": false,
    "minifyWXML": true,
    "babelSetting": { "outputPath": "" },
    "condition": false
  },
  "compileType": "miniprogram",
  "libVersion": "3.6.0",
  "appid": "你的微信小程序 AppID",
  "projectname": "承德应职院微墙",
  "condition": {}
}
```

---

## 十、实施步骤（AI 执行计划）

### 阶段一：环境搭建（人工）

```bash
# 1. 微信公众平台注册小程序 → 获取 AppID

# 2. 下载安装微信开发者工具

# 3. 开通 CloudBase（微信开发者工具 → 云开发 → 开通）

# 4. 安装 CLI 工具
npm install -g @cloudbase/cli
tcb login

# 5. 安装 Claude Code 的 MCP 工具
npx skills add tencentcloudbase/cloudbase-skills
```

### 阶段二：数据库迁移（AI 执行）

```
第 1 步：通过 CloudBase MCP 创建 3 个集合
  - posts
  - users
  - chat_messages

第 2 步：创建索引
  - posts.category + created_at
  - posts.reply_to
  - posts.user_email
  - posts.content (text)

第 3 步：将 PostgreSQL 数据导出为 JSON
  - 从当前数据库导出 messages, users, likes, favorites 表
  - 合并 likes 和 favorites 到 posts 的 liked_by, faved_by 数组
```

### 阶段三：云函数开发（AI 执行）

```
第 4 步：创建 cloudfunctions 目录
第 5 步：按第四章实现每个云函数
第 6 步：逐个部署测试
  tcb functions:deploy login
  tcb functions:deploy getPosts
  ...（共 14 个云函数）
```

### 阶段四：小程序页面（AI 执行）

```
第 7 步：创建 pages 目录结构（8 个页面）
第 8 步：按第五章实现每个页面的 WXML/WXSS/JS
第 9 步：实现组件（post-card, avatar）
第 10 步：实现 utils/api.js API 封装
```

### 阶段五：调试与发布（AI 执行 + 人工审核）

```
第 11 步：通过 weapp-agent-mcp 连接开发者工具
第 12 步：AI 自动运行测试用例
第 13 步：修复 bug
第 14 步：通过 yx-wechat-mini-cli 上传代码
第 15 步：人工 → 微信公众平台 → 提交审核
```

---

## 十一、关键差异对照

| 功能点 | Web 版 | 小程序版 |
|--------|-------|---------|
| 用户登录 | 邮箱密码 | 微信一键登录（免注册） |
| 帖子列表 | Flask 渲染 | 云函数 + WXML 渲染 |
| 点赞/收藏 | 独立的 likes/favorites 表 | posts 文档内的 liked_by/faved_by 数组 |
| 图片上传 | OSS + Pillow 压缩 | 云存储 + 自动压缩 |
| 搜索 | Elasticsearch / ILIKE | 云数据库正则查询 |
| 热榜 | PostgreSQL 聚合查询 | 云数据库排序查询 |
| 缓存 | Redis | 小程序本地缓存 + 云数据库 |
| 部署 | Docker + Railway | 云函数 + 微信开发者工具 |
| session | Flask session | 微信 openid |

---

## 十二、注意事项

1. **微信登录不需要密码**，用户首次打开小程序自动完成注册
2. **云数据库没有 auto-increment ID**，用 `_id` 替代
3. **云函数冷启动**：首次调用可能慢 1-2 秒，建议设置定时触发器预热
4. **云函数 10 秒超时**（可配到 30 秒），图片上传要单独设长超时
5. **小程序包大小限制 2MB**，云函数代码不计入
6. **管理员判定**：不再用邮箱，改为写死管理员 openid
7. **不支持直接 DOM 操作**，所有 UI 更新通过 setData
8. **不支持 jQuery/ajax**，用 wx.request 或云函数调用替代
9. **图片预览**：小程序用 wx.previewImage 替代浏览器的图片查看器
10. **下拉刷新**：小程序用 onPullDownRefresh 替代浏览器的 scroll 事件

---

## 十三、Web 版踩坑记录（小程序版直接避雷）

### 13.1 导航与路由

| Web 踩的坑 | 小程序直接避免 |
|-----------|----------------|
| **历史栈污染**：返回用了 location.href 产生新记录 | 小程序用 wx.navigateBack，不产生历史污染 |
| **搜索状态丢失**：页面跳转后关键词和滚动位置丢失 | 小程序用 onShow + globalData，页面切回自动恢复 |
| **bfcache 不兼容**：load 事件返回时不触发 | 小程序用 onShow，每次显示都触发 |
| **Tab 切换整页刷新**：用户体验差 | 小程序 Tab 切换不刷新，data 局部更新 |

### 13.2 数据库

| Web 踩的坑 | 小程序直接避免 |
|-----------|----------------|
| **ID 自增冲突**：手动插数据后 sequence 没更新 | 云数据库 _id 自动生成 |
| **列不存在**：加新字段要 ALTER TABLE | 文档型数据库，随时加字段不报错 |
| **JOIN 三张表**：likes/favorites 联表查询复杂 | 用 liked_by/faved_by 数组，一个文档搞定 |

### 13.3 用户认证

| Web 踩的坑 | 小程序直接避免 |
|-----------|----------------|
| **邮箱注册门槛高**：用户不愿意注册 | 微信一键登录，无感注册 |
| **Session 失效**：部署重启后用户要重新登录 | openid 永久有效，从不断登 |
| **退出登录状态残留**：退出后还有旧数据 | onShow 里重新检查登录态 |

### 13.4 图片处理

| Web 踩的坑 | 小程序直接避免 |
|-----------|----------------|
| **图片 404**：存本地 → Railway 重启丢失 | 云存储永久保存 |
| **要装 Pillow 压缩**：需要自己写压缩代码 | 云存储自动压缩 |
| **头像不同步**：旧帖子头像没更新 | 每次渲染读最新头像 URL |

### 13.5 部署运维

| Web 踩的坑 | 小程序直接避免 |
|-----------|----------------|
| **冷启动慢**：Railway 休眠后首次请求 5 秒 | 云函数常驻实例 |
| **内存不足**：1GB 跑 48 个 Celery 进程被 OOM 杀死 | 云函数自动扩缩容 |
| **端口配置麻烦**：Flask/Gunicorn/Docker 三处要一致 | 不需要管端口 |
| **AccessKey 泄露风险**：差点提交到 Git | 云函数环境变量，安全 |
| **Docker 构建慢**：每次 push 等 3 分钟 | 开发者工具秒传 |

---

## 十四、日活增长策略

### 14.1 微信小程序天然优势

| 优势 | 效果 |
|------|------|
| 无需下载、扫码即用 | 获客成本为 0 |
| 微信一键登录 | 注册率 +80% |
| 分享到聊天/朋友圈 | 用户自发传播 |
| 订阅消息（有人回复时通知） | 召回用户 |
| 小程序浮窗 | 随时回访 |
| 搜一搜直达 | 搜索流量 |

### 14.2 日活 0 → 1000

| 策略 | 做法 |
|------|------|
| 班级群裂变 | 帖子可分享到班级群 |
| 今日热榜 | Web 版已验证的留存利器 |
| 二手闲置 | 校园刚需，每天有人刷 |
| 免登录浏览 | 不登录也能看帖子 |

### 14.3 日活 1000 → 10000

| 策略 | 做法 |
|------|------|
| 订阅回复通知 | 有人回复 → 服务通知 → 点回来 |
| 话题标签 | #校园活动 #失物招领 更细分类 |
| 排行榜 | 今日发帖王、点赞王，激励活跃用户 |
| 课表/成绩查询 | 工具属性拉留存 |

### 14.4 留存指标参考

```
次日留存 > 40%  → 功能没问题
7日留存 > 20%   → 有粘性
30日留存 > 10%  → 可以做大了

次日低 → 首次体验不好（优化引导）
7日低  → 功能不够（加热榜/通知）
30日低 → 没有常驻理由（加工具属性）
```

---

## 十五、AI 提示词模板

### 15.1 初始启动提示词

```
我用 CloudBase MCP 开发微信小程序"承德应职院微墙"，
校园内容分享平台。
项目路径: [新文件夹路径]
AppID: [你的 AppID]
后端用云开发数据库 + 云函数。

请按以下步骤全自动完成：
1. 创建完整项目目录结构
2. 云数据库创建集合：posts、users、chat_messages，配索引
3. 实现所有云函数（login/getPosts/createPost/toggleLike等14个）
4. 实现所有页面（首页/详情/发布/我的/热榜/搜索/评论/登录）
5. 每个页面包含 WXML、WXSS、JS、JSON 完整文件
6. 每次写完自动调用开发者工具验证

参考文档：微信兼容网页版承德应用技术学院小程序实施步骤文档.md
所有经验教训和避坑指南都在里面。
```

### 15.2 调试修复提示词

```
云函数 XXX 报错：[贴报错]。
请读取代码分析原因，修复后重新部署。
已知此问题在 Web 版遇到过类似情况：[描述]
```

### 15.3 新增功能提示词

```
在首页加"今日热榜"入口，点击进入热榜页面，
按点赞+评论数排序显示前 10 条。
参考 Web 版 hot.html 的实现逻辑。
```

---

## 十六、30 万人日活架构方案

### 16.1 不用担心的（CloudBase 自动搞定）

| 能力 | 说明 |
|------|------|
| **服务器扩缩容** | 云函数自动扩缩，无需手动加机器 |
| **数据库读写分离** | 云数据库自动分片，无需配置主从 |
| **CDN 加速** | 云存储自带 CDN，全球节点 |
| **DDoS 防护** | 微信网关自带防护 |
| **SSL 证书** | 微信小程序强制 HTTPS，无需自己配 |

### 16.2 需要关注的

| 关注点 | 30 万日活需求 | 解决方案 |
|--------|-------------|----------|
| **云函数并发** | 峰值 500 QPS | 设置 `minReplicas=10` 常驻 10 个实例 |
| **云数据库 QPS** | 峰值 1000 读/秒 | 加二级索引 + 集合上限设 100 万 |
| **云存储流量** | 日均 50GB | 图片压缩 + CDN 缓存 |
| **搜索** | 日均 10 万次搜索 | 云数据库正则搜索够用，不够上 Elasticsearch |

### 16.3 云函数配置（30 万日活）

```json
{
  "functions": [
    {
      "name": "getPosts",
      "timeout": 10,
      "memory": 256,
      "minReplicas": 10,      // 常驻 10 个实例
      "maxReplicas": 100      // 峰值自动扩到 100
    },
    {
      "name": "createPost",
      "timeout": 10,
      "memory": 256,
      "minReplicas": 5,
      "maxReplicas": 50
    },
    {
      "name": "search",
      "timeout": 10,
      "memory": 512,
      "minReplicas": 5,
      "maxReplicas": 50
    },
    {
      "name": "getHot",
      "timeout": 10,
      "memory": 256,
      "minReplicas": 5,
      "maxReplicas": 50
    }
  ]
}
```

### 16.4 数据库扩缩容策略

| 数据量 | 集合上限 | 索引策略 | 说明 |
|-------|---------|---------|------|
| 0-1 万条 | 不设限 | category + created_at | 默认 |
| 1-10 万条 | 100 万 | 同上 + user_email | 增速查 |
| 10-100 万条 | 500 万 | 同上 + content 全文索引 | 加搜索索引 |
| 100 万以上 | 按时间分集合 | 按月分表 posts_202606 | 需业务代码配合 |

### 16.5 30 万日活成本估算

| 项目 | 免费额度 | 30 万日活用量 | 月费 |
|------|---------|-------------|------|
| 云函数调用 | 200 万次/月 | 3000 万次/月 | ≈ ¥200 |
| 云数据库读写 | 5GB/月 | 50GB/月 | ≈ ¥300 |
| 云存储 | 5GB/月 | 100GB/月 | ≈ ¥100 |
| CDN 流量 | 5GB/月 | 200GB/月 | ≈ ¥200 |
| **合计** | | | **≈ ¥800/月** |

相比 Web 版（¥11000-30000/月），省了 90% 以上。

### 16.6 降级策略

```javascript
// 云函数降级：高峰期自动降级非核心功能
exports.main = async (event) => {
  if (isPeakHour()) {
    // 高峰期：只返回必要字段，不统计
    return { code: 0, data: posts, degraded: true }
  } else {
    // 非高峰期：完整数据
    return { code: 0, data: fullPosts }
  }
}

// 前端降级：云函数超时时展示缓存数据
const getPostsWithFallback = async () => {
  const res = await call('getPosts').catch(() => null)
  if (res) return res
  // 云函数挂了，读本地缓存
  return wx.getStorageSync('cached_posts') || []
}
```

### 16.7 30 万日活小结

**微信小程序 + CloudBase 做 30 万日活不需要改架构，只需要调整云函数的 `minReplicas` 和 `maxReplicas`。**

Web 版需要搞读写分离、Redis 集群、Elasticsearch、Kubernetes……
小程序版：**云函数配几个参数就够了。**

---

## 十七、Web 版完整踩坑记录与专业术语表

### 17.1 前端导航类

| 问题 | 术语 | 说明 | 小程序如何避免 |
|------|------|------|--------------|
| 返回产生新记录 | **历史栈污染**（History Stack Pollution） | location.href 返回产生新历史记录 | 用 wx.navigateBack 回退 |
| 刷新后搜索没了 | **客户端状态丢失**（State Loss） | 页面跳转导致 JS 内存清空 | 用 globalData + onShow 恢复 |
| 返回不自动恢复 | **bfcache 不兼容** | 浏览器缓存页面，load 事件不触发 | 小程序 onShow 每次触发 |
| 关闭搜索又自动弹 | **状态残留**（State Leak） | sessionStorage 没清空 | 退出时清理 globalData |

### 17.2 数据库类

| 问题 | 术语 | 说明 | 小程序如何避免 |
|------|------|------|--------------|
| ID 自增冲突 | **序列不同步**（Sequence Desync） | 手动插数据后自增 ID 不连续 | 云数据库 _id 自动生成 |
| 列不存在报错 | **数据库迁移兼容** | 加新字段要 ALTER TABLE | 文档型数据库，随意加字段 |
| JOIN 三张表太慢 | **联表查询**（JOIN） | likes/favorites 要 JOIN 查询 | 用数组字段替代关联表 |
| 查询越来越慢 | **全表扫描**（Full Table Scan） | 没有索引时从头翻到尾 | 加上 category + created_at 索引 |
| 高并发连接不够 | **连接池耗尽**（Pool Exhaustion） | 数据库连接不够用 | 云数据库自动管理连接 |

### 17.3 缓存类

| 问题 | 术语 | 说明 | 小程序如何避免 |
|------|------|------|--------------|
| Redis 连不上崩了 | **降级**（Fallback） | cache=None 时自动走数据库 | 不需要 Redis，云数据库自带 |
| 缓存同时过期 | **缓存雪崩**（Cache Stampede） | 大量 key 同时过期 | 云数据库内置缓存 |
| 热点数据压力大 | **缓存穿透** | 频繁查同一条数据 | 小程序端缓存 + 云函数缓存 |

### 17.4 图片处理类

| 问题 | 术语 | 说明 | 小程序如何避免 |
|------|------|------|--------------|
| 部署后图片 404 | **本地存储不可靠** | 图片存本地，重启丢失 | 用云存储 |
| PNG 透明变白 | **格式转换丢失**（PNG Transparency Loss） | 统一转 JPEG 导致透明丢失 | 云存储原样保存 |
| 上传原图太大 | **图片体积过大**（Image Bloat） | 5MB 图片直接存 | 云存储自动压缩 |
| 头像不同步 | **头像同步遗漏**（Avatar Sync） | 改了头像旧帖子没更新 | 每次渲染读最新头像 URL |

### 17.5 部署运维类

| 问题 | 术语 | 说明 | 小程序如何避免 |
|------|------|------|--------------|
| Railway 重启后 Session 丢失 | **会话失效**（Session Invalidation） | 服务器重启后用户要重新登录 | openid 永久有效 |
| 1GB 内存不够 | **内存溢出**（OOM Kill） | Celery 48 并发被系统杀死 | 云函数自动扩缩容 |
| 首次请求慢 | **冷启动**（Cold Start） | 服务休眠后唤醒需要时间 | 设置 minReplicas 常驻 |
| 端口不匹配 502 | **端口不匹配**（Port Mismatch） | Docker 内外端口不一致 | 不需要管端口 |
| Gunicorn 超时 | **工作进程超时**（Worker Timeout） | 图片生成 >30 秒被断开 | 云函数最长可配 30 秒 |
| AccessKey 泄露风险 | **凭证泄露**（Credential Leak） | 差点把密钥提交到 Git | 用环境变量，不进代码 |

### 17.6 测试类

| 问题 | 术语 | 说明 | 小程序如何避免 |
|------|------|------|--------------|
| 测试找不到元素 | **测试漂移**（Test Drift） | 代码改了 UI，测试没更新 | 小程序用 weapp-agent-mcp 自动测 |
| Playwright 下载失败 | **网络限制**（Network Restriction） | 国内连不上 Google | 用微信开发者工具自带的调试能力 |
| 断言不准确 | **测试断言不准确** | 检查退出按钮比检查用户名更可靠 | 用 data 属性定位元素 |

### 17.7 专业术语速查表

| 术语 | 中文 | 解释 |
|------|------|------|
| **History Stack Pollution** | 历史栈污染 | 返回产生新记录，不减少历史栈 |
| **State Loss** | 状态丢失 | 页面跳转导致 JS 变量清空 |
| **bfcache** | 浏览器页面缓存 | 返回时从缓存恢复，load 事件不触发 |
| **State Leak** | 状态残留 | 关闭操作没清理持久化数据 |
| **Sequence Desync** | 序列不同步 | 手动插数据后自增 ID 不连续 |
| **OOM Kill** | 内存溢出被杀 | 进程占满内存被系统杀死 |
| **Cold Start** | 冷启动 | 服务长时间无请求后需重新唤醒 |
| **Full Table Scan** | 全表扫描 | 没有索引时从头翻到尾查数据 |
| **Cache Stampede** | 缓存雪崩 | 大量缓存同时过期导致数据库被打爆 |
| **Connection Pool** | 连接池 | 提前建好数据库连接，高并发复用 |
| **QPS** | 每秒查询数 | 衡量系统并发能力的指标 |
| **Fallback** | 降级 | Redis/ES 挂了自动走数据库，不崩溃 |
| **CDN** | 内容分发网络 | 静态资源缓存到离用户最近的节点 |
| **Master-Slave** | 主从复制 | 主库写数据，从库读数据 |
| **Read-Write Split** | 读写分离 | 写操作走主库，读操作走从库 |
| **Message Queue** | 消息队列 | 异步处理任务，削峰填谷 |
| **Microservices** | 微服务 | 把单体应用拆成多个独立服务 |
| **Kubernetes** | 容器编排 | 自动管理容器的扩缩容和部署 |
| **Index Scan** | 索引扫描 | 通过索引快速定位数据 |
| **TTL** | 过期时间 | Redis 缓存自动删除的时间 |
| **AccessKey** | 访问密钥 | 阿里云 API 的钥匙 |
| **Endpoint** | 接入点 | OSS/ES 的服务器地址 |
| **Bucket** | 存储桶 | OSS 里存文件的容器 |
| **Elasticsearch** | 搜索引擎 | 替代 ILIKE 模糊搜索，支持中文分词 |
| **smartcn** | 中文分词器 | ES 的智能中文分词，"今天天气"→"今天、天气" |
| **Celery** | 异步任务队列 | Python 后台任务处理 |
| **Prefork** | 预分支模式 | Celery 默认按 CPU 核心数启动并发 |
| **Concurrency** | 并发数 | 同时处理任务的数量 |
| **Session** | 会话 | 服务端记录用户登录状态 |
| **Token** | 令牌 | API 访问凭证 |
| **Rate Limiting** | 限流 | 控制每用户每秒请求次数 |

---

## 十八、技术栈全对比

### 18.1 后端技术

| 技术 | Web 版 | 小程序版 | 小程序优势 |
|------|-------|---------|-----------|
| 语言 | Python 3.13 | JavaScript (Node.js) | 前端统一语言 |
| 框架 | Flask 3.1 | 云函数 wx-server-sdk | 不用配路由 |
| 服务器 | Gunicorn 4 workers | 云函数自动扩缩容 | 不用管运维 |
| 数据库 | PostgreSQL | 云数据库（MongoDB） | 文档型，灵活 |
| 连接池 | psycopg2.pool 2-50 | 自动管理 | 省代码 |
| 缓存 | Redis | 云数据库内置缓存 | 省一个服务 |
| 图片存储 | OSS + 本地 | 云存储 | 省配置 |
| 搜索 | Elasticsearch | 云数据库正则 | 省部署 |
| 异步任务 | Celery Worker | 云函数异步 | 省一个服务 |
| Docker | 要写 Dockerfile | 不需要 | 省配置文件 |
| 部署 | Railway | 开发者工具上传 | 一步到位 |

### 18.2 前端技术

| 技术 | Web 版 | 小程序版 |
|------|-------|---------|
| 页面 | HTML + Jinja2 | WXML |
| 样式 | CSS | WXSS |
| 逻辑 | JavaScript | JavaScript |
| 请求 | fetch API | wx.cloud.callFunction |
| 缓存 | sessionStorage | wx.setStorageSync |
| 图片预览 | 自建弹窗 | wx.previewImage |
| 分享 | 复制链接 | wx.shareAppMessage |
| 登录 | 邮箱+密码 | wx.login 一键 |
| 导航 | location.href | wx.navigateTo |

### 18.3 数据库操作对照

| 操作 | PostgreSQL（Web 版） | 云数据库（小程序版） |
|------|--------------------|--------------------|
| 查询 | `SELECT * FROM messages WHERE category='daily'` | `db.collection('posts').where({category:'daily'}).get()` |
| 插入 | `INSERT INTO messages (content) VALUES ('xxx')` | `db.collection('posts').add({data:{content:'xxx'}})` |
| 更新 | `UPDATE messages SET likes_count=likes_count+1` | `db.collection('posts').doc(id).update({data:{likes_count:_.inc(1)}})` |
| 删除 | `DELETE FROM messages WHERE id=1` | `db.collection('posts').doc(id).remove()` |
| 分页 | `LIMIT 20 OFFSET 0` | `.skip(0).limit(20)` |
| 排序 | `ORDER BY created_at DESC` | `.orderBy('created_at','desc')` |
| 计数 | `SELECT COUNT(*) FROM messages` | `db.collection('posts').where({}).count()` |

---

## 十九、性能指标参考

### 19.1 性能标准

| 指标 | 好 | 一般 | 差 |
|------|-----|-----|-----|
| 页面首次加载 | < 1秒 | 1-3秒 | > 3秒 |
| 云函数响应 | < 200ms | 200-500ms | > 1秒 |
| 图片加载 | < 100KB | 100-300KB | > 500KB |
| 云数据库查询 | < 50ms | 50-200ms | > 500ms |
| 云函数冷启动 | < 500ms | 500ms-2s | > 2s |

### 19.2 小程序优化目标

| 指标 | 目标值 | 检查方法 |
|------|-------|---------|
| 首屏加载 | < 2秒 | 开发者工具 → Audit |
| 云函数响应 | < 300ms | 云函数日志耗时字段 |
| 图片大小 | < 100KB | 云存储控制台 |
| 页面切换 | < 500ms | 体验评分 |
| 内存占用 | < 200MB | 开发者工具 → Performance |

### 19.3 30 万日活容量规划

| 指标 | 每日总量 | 峰值每秒 |
|------|---------|---------|
| 页面访问（PV） | 600 万次 | 200 QPS |
| 帖子浏览 | 300 万次 | 100 QPS |
| 发帖 | 3 万次 | 10 QPS |
| 点赞/收藏 | 30 万次 | 50 QPS |
| 搜索 | 15 万次 | 30 QPS |
| 图片上传 | 1 万次 | 5 QPS |
| 云数据库读 | 1000 万次 | 500 QPS |
| 云数据库写 | 50 万次 | 100 QPS |

---

## 二十、完整项目功能清单

### 20.1 用户端功能

| 功能 | 优先级 | Web 版状态 | 小程序需实现 |
|------|--------|-----------|-------------|
| 用户注册/登录 | P0 | ✅ | 微信一键登录 |
| 浏览帖子列表 | P0 | ✅ | 云函数 getPosts |
| Tab 分类切换 | P0 | ✅ | 本地分类切换 |
| 发帖（文字+图片） | P0 | ✅ | 云函数 createPost |
| 点赞/取消 | P0 | ✅ | 云函数 toggleLike |
| 评论/回复 | P0 | ✅ | 云函数 reply |
| 搜索帖子 | P0 | ✅ | 云函数 search |
| 今日热榜 | P1 | ✅ | 云函数 getHot |
| 收藏/取消 | P1 | ✅ | 云函数 toggleFav |
| 个人中心 | P1 | ✅ | pages/my |
| 我的帖子列表 | P1 | ✅ | 云函数 getUserPosts |
| 我的收藏列表 | P1 | ✅ | 云函数 getUserFavs |
| 历史评论 | P2 | ✅ | 云函数 getUserComments |
| 编辑资料 | P2 | ✅ | 更新用户信息 |

### 20.2 管理员功能

| 功能 | Web 版 | 小程序版 |
|------|-------|---------|
| 删除帖子 | ✅ 按钮 | 云函数 deletePost |
| 管理后台 | 通过邮箱判定 | 通过 openid 判定 |

### 20.3 运营功能

| 功能 | 说明 |
|------|------|
| 客服（即将开放） | 微信客服组件 |
| 蹲贴（即将开放） | 订阅回复通知 |
| 跑腿收益（即将开放） | 暂不实现 |
| 官方客服 | 微信客服 |
| 本校客服 | 暂不实现 |

---

## 二十一、云函数核心代码模板

### 21.1 标准云函数模板

```javascript
const cloud = require('wx-server-sdk')
cloud.init()
const db = cloud.database()
const _ = db.command

exports.main = async (event, context) => {
  const { OPENID } = cloud.getWXContext()
  
  try {
    // 业务逻辑
    const result = await db.collection('posts').where({}).get()
    
    return {
      code: 0,
      data: result.data,
      openid: OPENID  // 调试用，生产可去掉
    }
  } catch (err) {
    console.error('[错误]', err)
    return {
      code: -1,
      error: '操作失败，请稍后重试'
    }
  }
}
```

### 21.2 云函数错误码规范

| 错误码 | 含义 | 前端处理 |
|-------|------|---------|
| 0 | 成功 | 正常显示数据 |
| -1 | 未知错误 | 提示"操作失败" |
| -2 | 参数错误 | 提示"参数不完整" |
| -3 | 权限不足 | 提示"没有权限" |
| -4 | 数据库错误 | 提示"系统繁忙" |
| -5 | 登录态过期 | 重新登录 |

### 21.3 前端统一错误处理

```javascript
// utils/api.js - 统一错误处理
const handleError = (err) => {
  if (err.code === -5) {
    // 登录过期，重新登录
    getApp().doLogin()
    return null
  }
  wx.showToast({ title: err.error || '操作失败', icon: 'none' })
  return null
}

const call = async (name, data) => {
  try {
    const res = await wx.cloud.callFunction({ name, data })
    if (res.result.code === 0) return res.result.data
    return handleError(res.result)
  } catch (err) {
    wx.showToast({ title: '网络开小差了', icon: 'none' })
    return null
  }
}
```

---

## 二十二、省钱替代方案

### 22.1 数据库替代方案

| 方案 | 费用 | 说明 | 适用阶段 |
|-----|------|------|---------|
| **CloudBase 云数据库**（推荐） | 免费 5GB，超了 ¥1/GB/月 | 和微信小程序天然集成 | 0-10 万日活 |
| ✅ 替代方案：JSON 文件存本地 | ¥0 | 小程序启动时从云存储下载 JSON | 0-100 日活（纯静态数据） |
| ✅ 替代方案：阿里云 MongoDB | ¥30/月 | 1GB 内存，不依赖 CloudBase | 已有阿里云账号可用 |

### 22.2 图片存储替代方案

| 方案 | 费用 | 说明 | 适用阶段 |
|-----|------|------|---------|
| **CloudBase 云存储**（推荐） | 免费 5GB，超额 ¥0.1/GB/月 | 和微信小程序天然集成 | 0-30 万日活 |
| ✅ 替代方案：阿里云 OSS | 免费 5GB，超额 ¥0.12/GB/月 | 已有账号，国内访问快 | 已有阿里云 |
| ✅ 替代方案：本地图片 Base64 | ¥0 | 小图片直接放代码里 | 0-100 日活测试用 |

### 22.3 搜索替代方案

| 方案 | 费用 | 说明 | 适用阶段 |
|-----|------|------|---------|
| **云数据库正则查询**（推荐） | 免费 | 小程序自带能力，不需要额外服务 | 0-1 万条帖子 |
| ✅ 替代方案：Elasticsearch | ¥200-500/月 | 需要自己部署或买 ES 服务 | 1 万条以上 |

### 22.4 服务器替代方案

| 方案 | 费用 | 说明 | 适用阶段 |
|-----|------|------|---------|
| **CloudBase 云函数**（推荐） | 免费 200 万次/月 | 和微信小程序天然集成 | 0-30 万日活 |
| ✅ 替代方案：阿里云函数计算 FC | 免费 100 万次/月 | 已有阿里云账号 | 已有阿里云 |
| ✅ 替代方案：最低配云服务器 | ¥50/月 | 1 核 1GB，自己部署 Node.js | 不依赖任何云平台 |

### 22.5 域名替代方案

| 方案 | 费用 | 说明 |
|-----|------|------|
| **微信小程序自带域名**（推荐） | ¥0 | 小程序用微信的域名，不需要自己买 |
| ✅ 替代方案：自己买域名 | ¥12-50/年 | 需要做 Web 管理端时用 |

### 22.6 推荐省钱组合

```
白嫖方案（0-1000 日活）：
  CloudBase 免费额度 → ¥0/月

低保方案（0-5 万日活）：
  CloudBase 云函数 ¥50 + 云存储 ¥30 + 云数据库 ¥30 → ¥110/月

小康方案（5-30 万日活）：
  CloudBase 升级套餐 + ES 搜索 ¥300 → ¥800/月

土豪方案（30 万+日活）：
  CloudBase 企业版 + 自有服务器混合部署 → ¥3000+/月
```

**建议先用白嫖方案，够用到 1000 日活一分钱不花。**

---

## 二十三、总结

**微信小程序 + CloudBase 比 Flask + PostgreSQL + Railway 简单 3 倍，坑少 5 倍，30 万日活成本省 90%。**

**微信小程序 + CloudBase 比 Flask + PostgreSQL + Railway 简单 3 倍，坑少 5 倍。**

| 对比 | Web 版 | 小程序版 |
|------|-------|---------|
| 开发时间 | 2 周 | 1 周（AI 干 6 小时） |
| 服务器成本 | ¥50-200/月 | ¥0（免费额度够用） |
| 用户门槛 | 要注册账号 | 微信一键登录 |
| 推广难度 | 要发链接 | 分享到聊天 |
| 需自建服务 | PostgreSQL/Redis/ES/OSS/Docker | 什么都不用建 |
| 维护成本 | 监控服务器、修复 bug | 不用管运维 |

Web 版 1500 行后端代码 + 3000 行前端 → 小程序版 800 行云函数 + 2500 行 WXML/WXSS。

