# 留言板项目文档

> 项目地址：https://devoted-adventure-production-b9ad.up.railway.app
> GitHub：https://github.com/3488351542/liu_yan_ban

---

## 一、项目结构

```
project1_message_board/
│
├── app.py                 # 主程序（Python + Flask + SQLite）
├── templates/
│   └── index.html         # 页面模板（HTML + CSS）
│
├── requirements.txt       # 依赖清单（告诉服务器要装哪些库）
├── Procfile               # 启动命令（告诉服务器怎么运行）
├── .gitignore             # Git 忽略规则（哪些文件不上传）
├── database.db            # 数据库文件（存留言数据，自动生成）
└── venv/                  # 虚拟环境（本地装库用，不上传）
```

| 文件 | 翻译 | 作用 |
|------|------|------|
| `app.py` | 应用 | 后端逻辑，处理请求、读写数据库 |
| `index.html` | 索引·超文本·标记·语言 | 页面样式和结构 |
| `requirements.txt` | 需求·文本 | pip install 时用 |
| `Procfile` | 过程·文件 | 服务器启动命令 |
| `.gitignore` | Git·忽略 | 让 Git 忽略 venv/、database.db |

---

## 二、app.py 核心逻辑（3 个数据库函数 + 2 个页面路由）

### 2.1 数据库函数

```
init_db()      初始化数据库，创建 messages 表
get_messages() 读取所有留言（按时间倒序）
save_message() 保存一条新留言
```

操作数据库的 5 步固定流程：

```
sqlite3.connect()   打开数据库文件
conn.cursor()       拿到操作手柄
cursor.execute()    执行 SQL 命令
conn.commit()       确认保存
conn.close()        关闭连接
```

### 2.2 页面路由

| 网址 | 函数 | 作用 |
|------|------|------|
| `http://.../` | `home()` | 首页，显示留言列表 |
| `http://.../submit` | `submit()` | 提交留言（仅 POST） |

### 2.3 SQL 命令（全部小写）

| SQL 命令 | 翻译 | 作用 |
|----------|------|------|
| `create table` | 创建·表 | 建表（只做一次） |
| `insert into ... values` | 插入·进入·值 | 存数据 |
| `select * from ... order by ... desc` | 查询·所有·从·排序·降序 | 取数据 |
| `delete from` | 删除·从 | 删数据（本阶段未用到） |

### 2.4 Flask 核心概念

| 代码 | 翻译 | 作用 |
|------|------|------|
| `@app.route("/")` | 路由 | 用户访问 `/` 时执行下面的函数 |
| `render_template()` | 渲染·模板 | 把数据传给 HTML 显示 |
| `request.form.get()` | 请求·表单·获取 | 取用户提交的表单数据 |
| `app.run(debug=True)` | 运行·调试 | 启动开发服务器 |

---

## 三、部署流程（Railway）

### 前提条件

```
1. GitHub 账号 + 代码已推送到仓库
2. Railway 账号（用 GitHub 登录）
```

### 部署步骤

```
1. 打开 https://railway.app，用 GitHub 登录
2. 点 New Project → Deploy from GitHub repo
3. 选择你的仓库（liu_yan_ban）
4. 如果提示授权，点 Configure GitHub App → 勾选仓库 → Save
5. 如果构建失败，检查 Settings → Branch 是否设置正确
6. 部署成功后会生成网址 https://xxx.up.railway.app
```

### 关键文件说明

| 文件 | 作用 | 为什么需要 |
|------|------|-----------|
| `requirements.txt` | 告诉服务器装 Flask、gunicorn | 不装就报 ModuleNotFoundError |
| `Procfile` | 告诉服务器运行 `gunicorn app:app` | 不写就不知道启动哪个文件 |

### 日常更新代码

```
git add .
git commit -m "改了什么"
git push origin master
# Railway 自动检测推送，自动重新部署
```

---

## 四、Git 命令速查

| 命令 | 翻译 | 作用 |
|------|------|------|
| `git init` | 初始化 | 让 Git 开始管这个文件夹 |
| `git remote add origin URL` | 远程·添加·来源 | 连上 GitHub 仓库 |
| `git pull origin master` | 拉取·来源·主分支 | 先下载 GitHub 上的变更 |
| `git add .` | 添加 | 把所有改过的文件装进箱子 |
| `git commit -m "说明"` | 提交 | 封箱并贴标签 |
| `git push origin master` | 推送·来源·主分支 | 寄到 GitHub |
| `git status` | 状态 | 看当前 Git 状态 |
| `git rm --cached 文件` | 移除·缓存 | 停止跟踪某个文件（不删本地） |

### 先拉再推的原因

```
GitHub 有提交 A，你本地有提交 B
→ 两个分支不一致，Git 拒绝推送
→ 先 git pull 合并，再 git push
```

### 首次使用需设置身份

```
git config --global user.email "你的邮箱"
git config --global user.name "你的用户名"
```

---

## 五、本地运行

```bash
cd project1_message_board
venv\Scripts\pip install -r requirements.txt
venv\Scripts\python app.py
# 打开 http://127.0.0.1:5000
```

---

## 六、常见问题

### Q: 部署后显示 Internal Server Error

原因：服务器运行时 `init_db()` 没有执行。
解决：把 `init_db()` 移到 `if __name__ == "__main__":` 外面（因为 gunicorn 导入时不执行 if 里面的代码）。

### Q: 数据库文件可以删除吗？

可以删。删了后重新运行 `app.py` 会自动重建。但已存的留言数据会丢失。

### Q: `.gitignore` 不生效？

如果文件已经上传到 GitHub 了，再加 `.gitignore` 也拦不住。需要：
```
git rm --cached 文件名     # 移除 Git 跟踪
git commit -m "移除跟踪"   # 提交变更
```

### Q: 本地的 main 还是 master？

GitHub 默认用 `main`，但你的仓库用了 `master`。
- 推送时用 `git push origin master`
- 在 Railway Settings 里把 Branch 设为 `master`
