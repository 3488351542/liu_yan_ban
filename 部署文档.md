# 部署文档

> 目标：把你的本地项目部署到 Railway，让别人通过网址访问

---

## 一、为什么需要部署

```
你的电脑                  →  别人访问不了（127.0.0.1 是本机地址）
用 Git 推到 GitHub       →  别人能看到你的代码，但用不了
Railway 从 GitHub 拉取   →  自动运行在云服务器上，24小时在线
用户访问 Railway 给的网址  →  任何人都能用留言板
```

---

## 二、前提条件

```
1. GitHub 账号（存放代码）
2. 代码已推送到 GitHub 仓库
3. Railway 账号（用 GitHub 登录，免费额度 $5/月）
```

---

## 三、部署步骤（共 6 步）

### 第 1 步：注册 Railway

```
1. 打开 https://railway.app
2. 点右上角 Login
3. 选 Continue with GitHub
4. 授权登录
```

### 第 2 步：创建项目

```
1. 点 New Project（新项目）
2. 选 Deploy from GitHub repo（从 GitHub 仓库部署）
3. 如果第一次用，点 Configure GitHub App
   → 跳转到 GitHub 授权页面
   → 选 Only select repositories
   → 勾选你的仓库（liu_yan_ban）
   → 点 Install & Authorize
4. 回到 Railway，选择你的仓库
```

### 第 3 步：设置分支

```
默认 Railway 部署 main 分支，如果你的仓库是 master：

1. 点 Settings（设置）
2. 找到 Branch（分支）
3. 把 main 改成 master（或你的分支名）
4. 自动触发重新部署
```

### 第 4 步：添加必要文件

> Railway 构建时需要知道两件事：① 装哪些库 ② 怎么启动

**requirements.txt — 依赖清单**
```
blinker==1.9.0
click==8.4.1
colorama==0.4.6
Flask==3.1.3
gunicorn==23.0.0
itsdangerous==2.2.0
Jinja2==3.1.6
MarkupSafe==3.0.3
Werkzeug==3.1.8
```

**Procfile — 启动命令**
```
web: gunicorn app:app
```

> 如果构建失败，先检查项目根目录有没有这俩文件

### 第 5 步：等待构建

```
构建日志显示：
  ✅ Initialization（初始化）
  ✅ Build（构建）
  ✅ Deploy（部署）
  ✅ Running（运行中）
```

### 第 6 步：找到网址

```
1. 点项目页面
2. 找 Domains（域名）或 Public URL
3. 网址格式：https://xxx.up.railway.app
4. 发给别人就能用了
```

---

## 四、部署后更新代码

每次改完代码：

```bash
git add .
git commit -m "改了什么"
git push origin master
```

Railway 自动检测到推送，**自动重新部署**，不需要手动操作。

---

## 五、常见部署问题

### Q: Build failed（构建失败）

**原因 1：没有 requirements.txt**
→ 项目根目录创建 `requirements.txt`，写上 Flask

**原因 2：没有 Procfile**
→ 项目根目录创建 `Procfile`，写上 `web: gunicorn app:app`

**原因 3：分支不对**
→ Railway 默认部署 main 分支，如果你的代码在 master 上
→ Settings → Branch → 改成 master

### Q: Internal Server Error（服务器内部错误）

**原因：** `init_db()` 在 `if __name__ == "__main__":` 里面
→ gunicorn 导入时不执行 if 里的代码，数据库未初始化
→ 修复：把 `init_db()` 移到 if 外面（模块级别）

```python
# 错误写法
if __name__ == "__main__":
    init_db()       # 只在 python app.py 时执行
    app.run()

# 正确写法
init_db()           # 导入时就会执行，gunicorn 能用
if __name__ == "__main__":
    app.run()
```

### Q: 免费额度不够了怎么办

```
Railway 免费：$5/月
留言板消耗：~$0.5/月
→ 一般够用

如果超出：
  1. 升级到 Hobby 方案（$5/月）
  2. 或者换 Zeabur / PythonAnywhere / 国内云服务器
```

---

## 六、关键词翻译

| 英文 | 拼音发音 | 中文 |
|------|---------|------|
| deploy | di pu luo yi 迪普洛伊 | 部署 |
| build | bi er de 比尔的 | 构建 |
| branch | bu ran chi 布染迟 | 分支 |
| settings | sai ting si 赛ting斯 | 设置 |
| domain | dou mei yin 斗梅因 | 域名 |
| URL | you ai er ou 优埃尔欧 | 网址 |
| log | lao ge 涝格 | 日志 |
| repo / repository | rui pao zi te rui 瑞泡兹特瑞 | 仓库 |
| push | pu shi 普石 | 推送 |
| pull | pu er 普尔 | 拉取 |
| commit | ke mi te 科密特 | 提交 |
| merge | me zhi 么之 | 合并 |
| main | mei yin 梅因 | 主分支 |
| master | ma si te 马斯特 | 主分支 |
| Procfile | pu ao si fai er 普奥斯法伊尔 | 进程文件 |
| requirements | rui kuai er men ci 瑞快尔门次 | 需求/依赖 |
