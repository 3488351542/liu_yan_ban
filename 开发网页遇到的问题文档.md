# 开发网页遇到的问题文档

> 记录"承德应职院微墙"项目开发过程中遇到的所有问题、专业术语和解决方案
> 更新时间：2026-06-08

---

## 一、前端导航类

### 1. 返回跳转首页（历史栈污染）

| 项目 | 内容 |
|------|------|
| **问题** | 从搜索点帖子→详情页，点←返回不是回退，而是跳转到首页，历史记录越来越多 |
| **术语** | **历史栈污染**（History Stack Pollution） |
| **原因** | 详情页的←按钮用了 `location.href = '/?from=search'`（跳转），不是 `history.back()`（回退） |
| **解决** | `history.back()` 回退到上一页，不产生新历史记录 |

### 2. 搜索返回后位置丢失（客户端状态丢失）

| 项目 | 内容 |
|------|------|
| **问题** | 搜索→点帖子→返回，搜索结果没有了，滚动位置也丢了 |
| **术语** | **客户端状态丢失**（Client-side State Loss） |
| **原因** | 页面跳转后 JS 内存变量全部清空，搜索状态没保存 |
| **解决** | 跳转前用 `sessionStorage.setItem()` 保存搜索内容和滚动位置，返回后 `restoreSearchState()` 恢复 |

### 3. 返回不自动恢复搜索（bfcache 不兼容）

| 项目 | 内容 |
|------|------|
| **问题** | 从详情页点←返回，搜索弹窗没有自动弹出来，要手动再点搜索 |
| **术语** | **bfcache 不兼容**（Back-Forward Cache Compatibility） |
| **原因** | 浏览器有页面缓存（bfcache），从缓存恢复时不触发 `window.addEventListener('load')` |
| **解决** | `window.addEventListener('pageshow')` 替代 `load`，pageshow 事件在 bfcache 恢复时也会触发 |

### 4. 关闭搜索后又自动弹出来（状态残留）

| 项目 | 内容 |
|------|------|
| **问题** | 关闭搜索弹窗后，下次打开页面或点返回，搜索又自动弹出来 |
| **术语** | **状态残留**（State Leak） |
| **原因** | `closeSearch()` 函数只隐藏弹窗，没清空 `sessionStorage`，`pageshow` 检测到有数据又打开了 |
| **解决** | 关闭时调用 `clearSearchState()` 清空 `sessionStorage` |

### 5. "我的"按钮反复让登录

| 项目 | 内容 |
|------|------|
| **问题** | 已登录状态点底部"我的"按钮，又跳到登录页面 |
| **术语** | **硬编码路由**（Hardcoded Route） |
| **原因** | 底部导航的"我的"链接写死 `<a href="/login">`，不管是否登录 |
| **解决** | `{% if email %}<a href="/">{% else %}<a href="/login">{% endif %}` 条件判断 |

---

## 二、帖子发布类

### 6. 发布后帖子显示在错误分类

| 项目 | 内容 |
|------|------|
| **问题** | 选择"日常投稿"发布，帖子却出现在"留言板"分类 |
| **术语** | **重定向错误**（Redirect Mismatch） |
| **原因** | `redirect(request.referrer)` 跳回来源页面，来源可能是其他分类 |
| **解决** | 改为 `redirect(f"/?category={category}")`，明确跳转到正确的分类 |

### 7. 回复后看不到内容

| 项目 | 内容 |
|------|------|
| **问题** | 在"日常投稿"帖子下回复，回复完跳转到"留言板"，看不到刚回复的内容 |
| **术语** | **上下文丢失**（Context Loss） |
| **原因** | 回复请求没传当前分类，默认跳到 message Tab |
| **解决** | AJAX 回复后跳回当前分类 `redirect(f"/?category={category}")` |

---

## 三、数据库类

### 8. serial 自增 ID 冲突

| 项目 | 内容 |
|------|------|
| **问题** | 手动 INSERT 测试数据时指定了 ID，但 sequence 没更新，新增报错"重复键违反唯一约束" |
| **术语** | **序列不同步**（Sequence Desynchronization） |
| **原因** | PostgreSQL 的 serial 字段用 sequence 自增，手工指定 ID 不会自动更新 sequence |
| **解决** | `SELECT setval('messages_id_seq', (SELECT MAX(id) FROM messages))` 同步序列 |

### 9. 旧数据库没有新字段

| 项目 | 内容 |
|------|------|
| **问题** | 项目升级加了新字段（category、likes_count 等），旧数据库报错"列不存在" |
| **术语** | **数据库迁移兼容**（Database Migration Compatibility） |
| **原因** | 没有做正式迁移脚本，直接改 CREATE TABLE 对旧库不生效 |
| **解决** | 在 `init_db()` 中用 `information_schema.columns` 检测缺失列，自动 ALTER TABLE ADD COLUMN |

### 10. psql 命令找不到

| 项目 | 内容 |
|------|------|
| **问题** | Windows PowerShell 输入 psql 提示"不是内部或外部命令" |
| **术语** | **PATH 未配置**（Environment Path Missing） |
| **原因** | PostgreSQL 安装后没有把 bin 目录加到系统环境变量 |
| **解决** | 将 `C:\Program Files\PostgreSQL\17\bin` 添加到系统 PATH |

---

## 四、部署类

### 11. Gunicorn 超时

| 项目 | 内容 |
|------|------|
| **问题** | 图片生成 API 跑超过 30 秒，Gunicorn 主动断开连接 |
| **术语** | **工作进程超时**（Worker Timeout） |
| **原因** | Gunicorn 默认 timeout=30 秒 |
| **解决** | `CMD ["gunicorn", "--timeout", "600", ...]` 改为 600 秒 |

### 12. Railway 端口不匹配

| 项目 | 内容 |
|------|------|
| **问题** | Railway 检查 8080 端口，但 Flask 监听 5000，返回 502 Bad Gateway |
| **术语** | **端口不匹配**（Port Mismatch） |
| **原因** | Railway 默认 Target Port 是 8080，与 Dockerfile 不一致 |
| **解决** | Railway Dashboard → Settings → Networking → Target Port 设为 5000 |

### 13. DATABASE_URL 未注入

| 项目 | 内容 |
|------|------|
| **问题** | Railway 加了 PostgreSQL 插件，但 `os.environ.get("DATABASE_URL")` 拿到空值 |
| **术语** | **环境变量缺失**（Environment Variable Missing） |
| **原因** | Railway 没有自动把 PostgreSQL 连接串注入到服务环境变量 |
| **解决** | Railway Dashboard → Variables → 手动 Refer PostgreSQL 的连接变量 |

---

## 五、测试类

### 14. Playwright 浏览器下载失败

| 项目 | 内容 |
|------|------|
| **问题** | `playwright install chromium` 下载到一半失败，国内网络连不上 Google 存储 |
| **术语** | **网络限制**（Network Restriction） |
| **原因** | Playwright 默认从 Google CDN 下载浏览器，国内网络被墙/限速 |
| **解决** | 不下载浏览器！用 `channel='chrome'` 指向系统已安装的 Chrome |

### 15. Selenium 无法连接

| 项目 | 内容 |
|------|------|
| **问题** | webdriver-manager 下载 ChromeDriver 超时/403 错误 |
| **术语** | **依赖不可用**（Dependency Unavailable） |
| **原因** | ChromeDriver 托管在 Google 服务器，国内网络不稳定 |
| **解决** | 弃用 Selenium，改用 Playwright + 系统 Chrome |

### 16. 测试代码中文引号报错

| 项目 | 内容 |
|------|------|
| **问题** | `"承德应职院微墙"` 中文引号（`"` 和 `"`）与 Python 字符串引号搞混 |
| **术语** | **字符编码混淆**（Character Encoding Confusion） |
| **原因** | 中文引号虽然看起来像英文引号，但实际是不同的 Unicode 字符 |
| **解决** | Python 字符串用英文单引号 `'承德应职院微墙'` |

### 17. 退出登录测试误判

| 项目 | 内容 |
|------|------|
| **问题** | 测试判断"退出后页面上不能有用户名"，但旧帖子内容包含邮箱前缀导致测试失败 |
| **术语** | **测试断言不准确**（Inaccurate Test Assertion） |
| **原因** | 检查"用户名不存在"不如检查"退出按钮不存在"可靠 |
| **解决** | 改为 `assert page.locator('.logout-link').count() == 0` 检查退出按钮 |

### 18. 回复测试找不到元素

| 项目 | 内容 |
|------|------|
| **问题** | 测试点"回复按钮"找不到，因为回复功能从首页改到了详情页 |
| **术语** | **测试与代码不同步**（Test-Implementation Drift） |
| **原因** | 代码改了 UI 结构，但测试没更新 |
| **解决** | 更新测试流程：先进入详情页 `/post/{id}`，然后在详情页填回复 |

---

## 六、搜索功能类

### 19. 搜索结果的点赞按钮点不了（searchLike 未定义）

| 项目 | 内容 |
|------|------|
| **问题** | 搜索结果里的帖子点赞按钮点不了，控制台报错 `searchLike is not defined` |
| **术语** | **函数作用域问题**（Function Scope Issue） |
| **原因** | `doSearch()` 里动态生成的 HTML 用了 `searchLike()`，但这个函数没定义在当前作用域 |
| **解决** | 在 `doSearch()` 内部定义 `searchLike()` 函数 |

### 20. 搜索界面空白太大

| 项目 | 内容 |
|------|------|
| **问题** | 点开搜索弹窗，内容区域大片空白 |
| **术语** | **布局计算错误**（Layout Calculation Error） |
| **原因** | 搜索弹窗背景占满全屏，但内容区域高度没正确计算 |
| **解决** | 内容区域用 `flex: 1; overflow-y: auto`，背景渐变固定 |

---

## 术语速查表

| 术语 | 中文 | 简解 |
|------|------|------|
| History Stack Pollution | 历史栈污染 | 返回时产生新记录，不减少历史栈 |
| Client-side State Loss | 客户端状态丢失 | 页面跳转导致 JS 变量清空 |
| bfcache Compatibility | 返回缓存兼容 | 浏览器缓存页面导致 load 事件不触发 |
| State Leak | 状态残留 | 关闭操作没清理持久化数据 |
| Sequence Desync | 序列不同步 | 手工插数据后自增 ID 不连续 |
| Worker Timeout | 工作进程超时 | 请求处理太久被服务器断开 |
| Port Mismatch | 端口不匹配 | 容器内外端口号不一致 |
| Test Drift | 测试漂移 | 代码改了测试没更新 |
| Tab State Loss | Tab状态丢失 | Tab页点帖子→返回后滚动位置没了 |
| Grid/List Toggle | 网格/列表切换 | 同一URL根据有无参数显示不同布局 |
| Pinyin Leak | 拼音泄露 | 前端 label 里写了拼音注释，用户能看到 |
| Image Bloat | 图片体积过大 | 上传原图不压缩，5MB 图片加载慢 |
