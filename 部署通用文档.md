# 项目部署通用文档

> 本文档适用：任何需要部署到云端的项目
> 作者：AI 辅助编写，记录所有踩过的坑
> 更新时间：2026-06-07

---

## 单词发音表

| 英文 | 拼音 | 中文 |
|------|------|------|
| deploy | di pu lao yi 迪普老伊 | 部署 |
| redeploy | rui di pu lao yi 瑞迪普老伊 | 重新部署 |
| build | bi er de 比尔德 | 构建 |
| Docker | da ke 达克 | 容器平台 |
| Railway | rei wei 瑞威 | 云端部署平台 |
| image | yi mi zhi 伊米之 | 镜像 |
| container | ken tei ne 肯忒讷 | 容器 |
| port | pao te 泡特 | 端口 |
| environment variable | yin wai re men te wei re bou 因外仁特威热伯 | 环境变量 |
| log | lao ge 涝格 | 日志 |
| crash | ke ra shi 克拉石 | 崩溃 |
| target port | ta ge te pao te 塔格特泡特 | 目标端口 |

---

## 一、部署前的准备

### 1.1 项目清单

部署前确认以下文件齐全：

```
项目根目录/
  ├── Dockerfile              ← [必选] 容器构建文件
  ├── requirements.txt        ← Python 项目依赖
  ├── .gitignore              ← 忽略不该上传的文件
  └── app.py / main.py / ...  ← 项目主文件
```

### 1.2 代码必须是最新的

```bash
# 确认本地代码已提交
git status                   # 显示 "nothing to commit, working tree clean"
git log --oneline -3         # 看最近3条提交

# 推送到 GitHub
git push origin master       # 或 main（看你用的什么分支）
```

### 1.3 .gitignore 必须配置好

```gitignore
# 常见需要忽略的内容：
*.db                        # 数据库文件（避免覆盖）
venv/                       # Python 虚拟环境
.env                        # 环境变量文件（密钥）
__pycache__/                # Python 缓存
*.log                       # 日志文件
*.tar                       # 镜像包
.vscode/                    # IDE 配置
.idea/                      # PyCharm 配置
```

**注意：** 如果一个文件之前已经被 Git 跟踪了（比如 `database.db`），加 `.gitignore` 不会自动忽略它。需要先：

```bash
git rm --cached 文件名       # 取消跟踪（不删本地文件）
```

验证是否生效：

```bash
git check-ignore -v 文件名
# 有输出 = 已被忽略 ✅
# 没输出 = 还没被忽略 ❌
```

---

## 二、Dockerfile 编写规范

### 2.1 基础模板（Python 项目）

```dockerfile
# 第 1 行：基于哪个基础镜像
FROM python:3.13-slim

# 第 2 行：工作目录（容器里的路径）
WORKDIR /app1

# 第 3 行：复制依赖文件并安装（放代码前面，利用缓存）
COPY requirements.txt .
RUN pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# 第 4 行：复制项目代码
COPY . .

# 第 5 行：声明端口
EXPOSE 5000

# 第 6 行：启动命令
CMD ["gunicorn", "app:app", "-b", "0.0.0.0:5000"]
```

### 2.2 Dockerfile 逐行说明

| 行 | 指令 | 作用 | 常见坑 |
|:--|:-----|:-----|:-------|
| 1 | `FROM` | 选择基础镜像 | 国内需配镜像加速器 |
| 2 | `WORKDIR` | 设置容器里的当前目录 | 不影响性能，随便起名 |
| 3 | `COPY requirements.txt .` | 复制依赖文件 | 必须先于代码复制，用缓存加速 |
| 4 | `RUN pip install` | 安装依赖 | 国内用清华源 `-i https://pypi.tuna.tsinghua.edu.cn/simple` |
| 5 | `COPY . .` | 复制全部代码 | 前面有 .dockerignore 可以排除文件 |
| 6 | `EXPOSE` | 声明端口 | 只是声明，不映射，真正映射靠 `-p` |
| 7 | `CMD` | 启动命令 | 必须用 `0.0.0.0:5000`，不能用 `127.0.0.1` |

### 2.3 `0.0.0.0` vs `127.0.0.1` 的区别

```
127.0.0.1 = 只允许容器内部访问 → 浏览器打不开 ❌
0.0.0.0   = 允许外部访问       → 浏览器能打开 ✅
```

### 2.4 Gunicorn 常见坑

```dockerfile
# 如果 Flask session 过大，需要加参数：
CMD ["gunicorn", "app:app", "-b", "0.0.0.0:5000",
     "--limit-request-field_size", "16380",
     "--limit-request-line", "16380"]

# 如果文件叫别的名字：
# app:app  ＝ 文件名:Flask实例名
# 比如 main.py 里 app = Flask(__name__)
# 就是 main:app

# 如果在子目录：
# 比如 backend/app.py
# 就是 backend.app:app（用点号代替斜杠）
```

### 2.5 `app:app` 拆解

```
app:app
 ↑    ↑
文件名  Flask 实例变量名
       └── app.py 里有 app = Flask(__name__)
```

---

## 三、Docker 部署（传统方式）

### 3.1 构建镜像

```bash
docker build -t 项目名:版本号 .
```

| 参数 | 说明 |
|:-----|:------|
| `-t` | tag = 给镜像起名 |
| `项目名:版本号` | 如 `message-board:1.0` |
| `.` | 当前目录（Dockerfile 位置） |

**注意末尾的 `.` 不能少**，表示把当前目录作为构建上下文。

### 3.2 运行容器

```bash
docker run -d -p 本机端口:容器端口 --name 容器名 镜像名:版本号
```

示例：

```bash
docker run -d -p 5000:5000 --name my-board message-board:1.0
```

| 参数 | 说明 |
|:-----|:------|
| `-d` | 后台运行 |
| `-p 5000:5000` | 本机5000端口 → 容器5000端口 |
| `--name` | 给容器起名字 |

### 3.3 查看运行状态

```bash
docker ps               # 查看运行中的容器
docker ps -a            # 查看所有容器（包括已停止的）
docker images           # 查看所有镜像
docker logs 容器名      # 查看日志
docker logs -f 容器名   # 实时跟踪日志
```

### 3.4 停止和删除

```bash
docker stop 容器名
docker rm 容器名
docker rmi 镜像名:版本号
```

### 3.5 修改代码后重新部署

```bash
docker stop my-board
docker rm my-board
docker rmi message-board:1.0
docker build -t message-board:1.0 .
docker run -d -p 5000:5000 --name my-board message-board:1.0
```

### 3.6 打包发给别人

```bash
# 导出镜像为 .tar 文件
docker save -o 项目名.tar 镜像名:版本号

# 别人导入
docker load -i 项目名.tar
docker run -d -p 5000:5000 --name 容器名 镜像名:版本号
```

---

## 四、Railway 部署（云端平台）

### 4.1 创建项目

1. 打开 https://railway.app/，用 GitHub 登录
2. 点 **New Project** → **Deploy from GitHub repo**
3. 选择你的项目仓库

### 4.2 分支设置

在 **Settings** 里确认：

| 设置项 | 建议值 |
|:-------|:-------|
| Branch connected to production | `master` 或 `main`（看你用的哪个） |
| Auto deploys when pushed to GitHub | ✅ 开启 |

### 4.3 GitHub 推送确认

```bash
# 推送代码到 GitHub
git add .
git commit -m "改了什么"
git push origin master

# 确认 GitHub 上是最新的
git log origin/master --oneline -3
```

**常见坑：** 如果本地有 `master` 分支但 GitHub 默认是 `main` 分支，Railway 连接的是 `master` 分支但你的代码推到了其他地方。需要：

```bash
# 把 master 的代码同步到 main
git push origin master:main
```

### 4.4 添加数据库

点 **New** → **Database** → 选择你要的数据库：

| 数据库 | 适用场景 |
|:-------|:---------|
| PostgreSQL | 企业级网站 |
| MySQL | 网站（免费版已下线） |
| Redis | 缓存 |

Railway 会自动安装并生成连接地址（环境变量）。在 PostgreSQL 设置页面可以看到：

```
来源镜像：ghcr.io/railwayapp-templates/postgres-ssl:18
端口：5432
DATABASE_URL：自动生成
```

### 4.5 设置环境变量（DATABASE_URL）

这是**最容易出错的一步**，必须按顺序操作：

#### 第 1 步：找到 Variables 页面

点击你的 **app 服务**（不是 PostgreSQL 数据库服务）→ **Variables** 标签

#### 第 2 步：添加变量

| 方式 | 操作 |
|:-----|:------|
| 推荐 | 点 **Add Reference** → 选择 PostgreSQL 服务 → 选 `DATABASE_URL` |
| 手动 | 点 **Add Variable** → 名称填 `DATABASE_URL` → 值填 `${{Postgres.DATABASE_URL}}` |

添加后应该看到：

```
DATABASE_URL    ${{Postgres.DATABASE_URL}}
```

#### 第 3 步：保存并部署

点 **Apply 1 change** → 或 **Deploy**

#### ⚠️ 常见坑：变量加了没生效

```
症状：代码里明明有 os.environ.get("DATABASE_URL")，但日志还是连 localhost
原因：变量添加后没有触发重新部署
解决：必须去 Deployments → Redeploy（重新构建，不是重新启动）
```

#### 代码里必须这样写（兼容本地和云端）

```python
import os

def get_db():
    database_url = os.environ.get("DATABASE_URL")
    if database_url:
        # 云端：用平台提供的连接地址
        return psycopg2.connect(database_url)
    # 本地：用本机配置
    return psycopg2.connect(
        host="localhost",
        port=5432,
        database="my_db",
        user="postgres",
        password="123456"
    )
```

### 4.6 设置 Target Port（最容易踩的坑）

#### 原理

```
Railway 外网 → Target port → 你的应用
                ↑
           Railway 把外部请求转发到容器里的这个端口

你必须确保：
  Target port == 你的应用监听的端口
      ↓               ↓
    Settings里设的     Dockerfile里写的
```

#### 正确配置

| 你的应用监听端口 | Dockerfile 写法 | Target port 设置 |
|:------------:|:----------------|:----------------:|
| **5000** | `CMD ["gunicorn","app:app","-b","0.0.0.0:5000"]` | **5000** |
| **3000** | Node.js 默认 | **3000** |
| **8080** | 自定义 | **8080** |

去 **Settings** → **Networking** → **Target port** 设置，然后点 **Update**。

#### ⚠️ 常见坑：Target port 不对

```
错误现象：
  - Railway 状态显示 ✅ Active
  - 浏览器访问显示 502 Bad Gateway
  - 应用实际已经跑起来了

排查步骤：
  1. 去 Settings → Networking → 看 Target port 是多少
  2. 去 Dockerfile → 看 EXPOSE 和 CMD 里端口是多少
  3. 如果不一致 → 改 Target port 为 Dockerfile 里的端口

为什么默认是 8080？
  Railway 默认 Target port 是 8080
  但 Flask 默认监听 5000（或你在 CMD 里指定的端口）
  两者不一致 → 502
```

### 4.7 部署

#### 自动部署

推送代码到 GitHub 后，Railway 会自动开始部署：

```bash
git add .
git commit -m "改了什么"
git push origin master
```

#### 手动重新部署（Redeploy）

在以下情况必须手动 Redeploy：

| 情况 | 说明 |
|:-----|:------|
| 改了环境变量 | 变量不会自动触发部署 |
| 改了 Target port | 端口不会自动触发部署 |
| 应用崩溃后修复 | 修复代码后需要重新构建 |

操作步骤：

```
1. 点顶部 Deployments 标签
2. 找到最近一次部署记录
3. 点右边的 ⋮（三个点）
4. 选 Redeploy
```

**注意：** Redeploy 是重新构建 + 启动，不是重启。重启解决不了代码和环境问题。

### 4.8 查看日志（找错的关键）

#### 三种日志的区别

| 日志类型 | 在哪里看 | 看什么 |
|:---------|:---------|:-------|
| **Build Logs** | 部署记录 → Build Logs | Docker 构建是否有报错、依赖是否装成功 |
| **Deploy/Runtime Logs** | 部署记录 → Deploy Logs | 应用启动后的输出、Python 报错 |
| **HTTP Logs** | HTTP Logs 标签 | 每个请求的状态码和耗时 |

#### 怎么看

```
Deployments → 点击部署记录（Active 或 Crashed）→ 自动显示 Build + Deploy 日志
```

#### 典型报错识别

| 日志关键字 | 问题 | 章节 |
|:-----------|:-----|:----|
| `psycopg2.OperationalError: Connection refused` | 数据库连不上 | 4.9.1 |
| `ModuleNotFoundError` | 依赖没装 | 4.9.3 |
| `connection to server at "localhost"...failed` | DATABASE_URL 没配好 | 4.9.1 |
| `(^C|broken pipe): Connection refused` | 端口不对 | 4.9.2 |

### 4.9 报错排查手册

#### 错误 1：数据库连不上（Connection refused）

```
完整报错：
  psycopg2.OperationalError: connection to server at "localhost" (::1), port 5432 failed
  connection to server at "localhost" (127.0.0.1), port 5432 failed
  Is the server running on that host and accepting TCP/IP connections?

原因：
  应用还在尝试连接本地的 PostgreSQL（localhost:5432），而不是云端的

排查步骤：
  □ 步骤 1：确认 PostgreSQL 数据库服务已添加
    → 项目页面有没有 PostgreSQL 服务？
    → 没有 → New → Database → PostgreSQL

  □ 步骤 2：确认 DATABASE_URL 变量已添加
    → 去 app 服务的 Variables 页面
    → 有没有 DATABASE_URL = ${{Postgres.DATABASE_URL}} ？
    → 没有 → Add Reference → 选 PostgreSQL → 选 DATABASE_URL

  □ 步骤 3：确认变量添加后已 Redeploy
    → 变量加了不等于生效了
    → 必须手动 Deployments → Redeploy

  □ 步骤 4：确认代码读取了 DATABASE_URL
    → 检查 app.py 里 get_db() 函数
    → 有没有 os.environ.get("DATABASE_URL") 判断？
    → 没有 → 加上（见章节 4.5 代码示例）

  □ 步骤 5：确认 GitHub 上有最新代码
    → git log origin/master --oneline -3
    → 能看到 "支持DATABASE_URL" 之类的提交吗？
    → 没有 → git push origin master
```

#### 错误 2：502 Bad Gateway

```
完整报错：
  浏览器显示 "502 Bad Gateway"
  Request ID: xxxxxx

原因：
  Railway 把请求转发到某个端口，但你的应用不在那个端口监听

排查步骤：
  □ 步骤 1：看应用状态
    → Deployments → 是 Active 还是 Crashed？
    → Crashed → 看错误 4
  
  □ 步骤 2：检查 Target port
    → Settings → Networking → Target port 是多少？
    → Dockerfile 里 EXPOSE 和 CMD 的端口是多少？
    → 两者必须一样！

  □ 步骤 3：举几个典型匹配
    应用监听 5000（Flask 默认）→ Target port = 5000
    应用监听 3000（Node 默认） → Target port = 3000
    应用监听 8080               → Target port = 8080

  □ 步骤 4：改了 Target port 后
    → 等 Railway 自动重启（几秒钟）
    → 刷新浏览器

防止再踩坑：
  Dockerfile 里的 CMD 写什么端口，Target port 就设什么端口
  永远不要盲目相信默认值（Railway 默认是 8080）
```

#### 错误 3：Module Not Found

```
完整报错：
  ModuleNotFoundError: No module named 'xxx'

原因：
  requirements.txt 里没写全依赖，或者写错名字了

解决：
  □ 本地安装测试
    pip install xxx
    pip freeze > requirements.txt

  □ 确认 requirements.txt 包含了所有 import 的库
    # 看 app.py 里 import 了什么
    import flask          → 写 Flask
    import psycopg2       → 写 psycopg2-binary
    import requests       → 写 requests
    import qrcode         → 写 qrcode
```

#### 错误 4：应用崩溃（Crashed）

```
完整报错：
  Deployments 显示 Crashed
  Worker failed to boot.

原因：
  应用启动时就报错了，根本跑不起来

排查步骤：
  □ 步骤 1：看 Deploy Logs 最后的报错
    → 点部署记录 → Deploy Logs
    → 翻到最后，看最开始的报错（不是后面的重复错误）

  □ 步骤 2：识别常见崩溃原因
    ┌─────────────────────────────────────────────────────────────┐
    │ 报错关键字 → 原因 → 解决                                       │
    ├─────────────────────────────────────────────────────────────┤
    │ connection to server at "localhost"                         │
    │ → 数据库连不上 → 看错误 1                                     │
    ├─────────────────────────────────────────────────────────────┤
    │ ModuleNotFoundError                                         │
    │ → 缺依赖 → 看错误 3                                          │
    ├─────────────────────────────────────────────────────────────┤
    │ SyntaxError                                                 │
    │ → 语法错误 → 本地 python app.py 跑一下看报错                    │
    ├─────────────────────────────────────────────────────────────┤
    │ port 8080 conflict / address already in use                 │
    │ → 端口冲突 → 等一会再 Redeploy                                 │
    ├─────────────────────────────────────────────────────────────┤
    │ worker failed to boot                                       │
    │ → 通用启动失败 → 看上面更具体的报错                             │
    └─────────────────────────────────────────────────────────────┘

  □ 步骤 3：本地先测试
    python app.py           # 本地能跑吗？
    # 如果本地报错，云端肯定也报错

  □ 步骤 4：修复后重新推送
    git add .
    git commit -m "修复了xxx"
    git push origin master
    # Railway 会自动重新部署
```

#### 错误 5：应用 Active 但返回 502

```
完整报错：
  应用状态显示 ✅ Active
  浏览器访问还是 502

原因：
  应用跑起来了，但 Target port 和应用的监听端口不一致

排查：
  □ 检查 Target port（Settings → Networking）
  □ 检查 Dockerfile 里的端口
  □ 两者必须一致

示例：
  Dockerfile: CMD ["gunicorn","app:app","-b","0.0.0.0:5000"]
  Target port: 5000（不是 8080）
```

### 4.10 应用代码适配（完整示例）

```python
# 1. import 要加 os
import os
import psycopg2

# 2. get_db() 必须同时支持本地和云端
def get_db():
    """连接 PostgreSQL（支持本地和云端）"""
    database_url = os.environ.get("DATABASE_URL")
    if database_url:
        # 云端：Railway 会自动设置 DATABASE_URL
        return psycopg2.connect(database_url)
    # 本地：用你本机的 PostgreSQL 配置
    return psycopg2.connect(
        host="localhost",
        port=5432,
        database="message_board",
        user="postgres",
        password="123456"
    )

# 3. 在 app.py 底部，init_db() 在模块加载时执行
#    确保 init_db() 不阻塞应用启动
init_db()
```

### 4.11 完整部署核对清单

每次部署前，按顺序检查：

```
□ 1. 代码已提交推送
   git status → nothing to commit
   git push origin master

□ 2. GitHub 上是最新代码
   git log origin/master --oneline -3

□ 3. 如果有数据库 → DATABASE_URL 已添加
   在 Variables 页面能看到 DATABASE_URL = ${{Postgres.DATABASE_URL}}

□ 4. 如果改了环境变量 → 已 Redeploy
   变量加了后必须手动触发重新部署

□ 5. Target port 和应用端口一致
   去 Settings → Networking → Target port 确认

□ 6. Dockerfile 里的端口和 EXPOSE 一致
   CMD 写 5000 → EXPOSE 5000 → Target port 5000

□ 7. 查看日志确认部署成功
   Deploy Logs 没有报错

□ 8. 浏览器访问确认能打开
   刷新页面，看能不能正常显示

□ 9. 功能测试
   注册、登录、留言等基本功能正常

□ 10. 回滚方案准备
    记下当前部署 ID，出问题可以 Redeploy 旧版本
```

---

## 五、部署状态速查

### 5.1 应用状态

| 状态 | 含义 | 怎么办 |
|:-----|:------|:-------|
| ✅ Active | 运行中 | 正常 |
| ❌ Crashed | 崩溃了 | 看日志找错 |
| 🕐 Deploying | 正在部署 | 等待 |
| 🕐 Starting Container | 容器启动中 | 等 10-20 秒 |
| 502 Bad Gateway | 连不上应用 | 检查 Target port / 查看日志 |

### 5.2 常见错误速查表

| 症状 | 原因 | 解决方法 |
|:-----|:-----|:---------|
| Docker build 慢 / 超时 | 国内连 Docker Hub 被墙 | 配置镜像加速器（daemon.json 加 registry-mirrors） |
| `Connection refused` | 数据库没启动或地址不对 | 检查数据库服务状态 + 环境变量 |
| 数据库文件被覆盖 | Docker 重新构建时 `.db` 文件被替换 | 切换 PostgreSQL（独立服务）或用数据卷 |
| Git 推送后 Railway 没反应 | 分支不对或没触发 auto-deploy | 检查 Settings 分支设置，或手动 Redeploy |
| 变量改了没生效 | 改了变量没 Redeploy | 手动触发重新部署 |
| 本地能跑云端报错 | 依赖没装全或环境变量没配 | 检查 requirements.txt 和环境变量 |
| `Request Header Fields Too Large` | Flask session 太大 | gunicorn 加 `--limit-request-field_size 16380` |
| `git push` 说 `Everything up-to-date` | 没有新 commit | 先 `git add .` + `git commit` 再 push |

---

## 六、环境变量管理

### 6.1 为什么需要环境变量

```
写死在代码里的问题：
  host="localhost"       ← 本地能用，云端就崩了
  password="123456"      ← 密码暴露在代码里

用环境变量：
  host=os.environ.get("DB_HOST")   ← 本地、云端各自设置
  password=os.environ.get("DB_PASSWORD")  ← 密码不在代码里
```

### 6.2 代码里读取环境变量

```python
import os

value = os.environ.get("变量名")          # 获取变量，没有就返回 None
value = os.environ.get("变量名", "默认值") # 获取变量，没有就用默认值
value = os.environ["变量名"]              # 获取变量，没有就报错
```

### 6.3 哪些东西应该用环境变量

| 应该用环境变量 | 不应写死在代码 |
|:---------------|:---------------|
| 数据库连接地址 | API 端点路径（不变） |
| 数据库密码 | 超时时间 |
| API Key | 功能逻辑 |
| 不同环境的配置（开发/生产） | 错误提示文案 |

---

## 七、数据库部署决策

### 7.1 选型对比

| 对比 | SQLite（文件型） | PostgreSQL（服务型） |
|:-----|:---------------|:--------------------|
| 数据在哪 | 一个 `.db` 文件 | 独立后台服务 |
| 本地开发 | ✅ 简单，不用装 | ❌ 需要装 PostgreSQL |
| Docker 部署 | ❌ 文件会被覆盖 | ✅ 数据独立存在 |
| 云端部署 | ❌ 不推荐 | ✅ Railway 一键创建 |
| 并发 | ❌ 同时只能一个人写 | ✅ 多人同时访问 |
| 适合场景 | 小工具、手机 App | 网站、企业应用 |

### 7.2 如果用 SQLite

- ✅ 本地开发方便
- ❌ Docker 重新构建时数据会丢
- ❌ 推送部署可能会被覆盖（即使 `.gitignore` 了，Docker 重新构建也会产生新的空文件）

### 7.3 如果用 PostgreSQL

- ❌ 本地需要另外装 PostgreSQL
- ✅ 云端 Railway 一键添加
- ✅ 数据独立，不管怎么推送、怎么重新部署，数据都在

---

## 八、国内网络特殊处理

### 8.1 Docker 镜像加速

在国内 Docker 构建时，下载基础镜像会超时。需要配置加速器：

```json
// 文件位置：C:\Users\你的用户名\.docker\daemon.json
{
  "registry-mirrors": [
    "https://docker.xuanyuan.me",
    "https://docker.1ms.run",
    "https://docker.m.daocloud.io",
    "https://dockerproxy.link",
    "https://docker.jiaxin.site",
    "https://dc.j8.work",
    "https://docker.nju.edu.cn",
    "https://atomhub.openatom.cn"
  ]
}
```

配置后重启 Docker Desktop 生效。

### 8.2 Python 包镜像

```bash
# 安装依赖时用清华源
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# 也可以在 Dockerfile 里写
RUN pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

---

## 九、部署后的验证

部署完成后，按以下顺序检查：

```bash
# 1. 应用能访问
curl http://你的域名（或 localhost:端口）

# 2. 页面能正常打开
# 浏览器打开 http://你的域名

# 3. 数据库能读写
# 在应用里注册一个用户 / 发一条留言，看是否成功

# 4. 查看日志有无报错
docker logs 容器名       # Docker 部署
# 或 Railway 的 Runtime Logs

# 5. 检查环境变量是否生效
# 在 Railway Console 里：
printenv | grep DATABASE
```

---

## 十、回滚方案

### 10.1 Git 版本回退（代码回滚）

```bash
# 安全回滚（推荐）
git revert HEAD          # 撤销最新一次提交
git push origin master   # 推送

# 暴力回滚（慎用，会删历史）
git reset --hard 好版本id
git push origin master --force
```

### 10.2 Docker 镜像回滚

```bash
# 用旧版本的镜像重新运行
docker run -d -p 5000:5000 --name my-board 旧镜像名:旧版本号
```

### 10.3 Railway 回滚

```
Deployments → 找到之前的部署记录 → Redeploy
```

---

## 十一、关键词速查

| 关键词 | 作用 | 常用值 |
|:-------|:-----|:--------|
| `FROM` | 基础镜像 | `python:3.13-slim` |
| `WORKDIR` | 工作目录 | `/app` 或 `/app1` |
| `COPY` | 复制文件 | `COPY . .` |
| `RUN` | 构建时执行 | `RUN pip install ...` |
| `EXPOSE` | 声明端口 | `EXPOSE 5000` |
| `CMD` | 启动命令 | `CMD ["gunicorn", "app:app", "-b", "0.0.0.0:5000"]` |
| `-p` | 端口映射 | `-p 5000:5000` |
| `-d` | 后台运行 | |
| `-t` | 镜像起名 | `-t 项目名:版本号` |
| `DATABASE_URL` | 数据库连接地址 | `${{Postgres.DATABASE_URL}}` |
| `Target port` | Railway 目标端口 | 和应用监听端口一致 |
