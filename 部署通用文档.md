# 项目全貌文档 - 承德应职院微墙

> 本文档写给 AI 或开发者阅读，包含项目的完整架构、设计决策、踩坑记录
> 更新时间：2026-06-08
> 作者：AI 辅助编写

---

## 目录

1. [项目概述](#1-项目概述)
2. [技术栈](#2-技术栈)
3. [数据库设计](#3-数据库设计)
4. [页面结构与路由](#4-页面结构与路由)
5. [页面跳转流程](#5-页面跳转流程)
6. [API 接口文档](#6-api-接口文档)
7. [自动化测试](#7-自动化测试)
8. [缓存策略（Redis）](#8-缓存策略redis)
9. [开发中遇到的问题](#9-开发中遇到的问题)
10. [设计决策记录](#10-设计决策记录)
11. [部署指南](#11-部署指南)

---

## 1. 项目概述

### 1.1 这是什么
"承德应职院微墙"是一个校园内容分享平台，类似于简易版小红书/微博。
用户可以在平台上发布不同类型的帖子（留言、日常投稿、二手闲置），
其他用户可以点赞、评论、搜索内容。

### 1.2 发展历程
```
v1 - 简单留言板（Flask + SQLite）
v2 - 迁移到 PostgreSQL
v3 - 增加 AI 对话 + 图片生成功能
v4 - 微墙改版（分类系统/Tab切换/底部导航）
v5 - 新增：点赞、搜索、热榜、独立评论页（第一期）
v6 - 第二期：我的页面、收藏、今日热榜独立页、历史评论、头像上传、功能入口网格
v6.1 - 图片压缩优化、缓存头、移除前端拼音
v6.2 - 头像同步到帖子/评论、←按钮统一 history.back()、登录退出入口优化（当前版本）
```

### 1.3 项目定位
- 目标日活：5000 人
- 校园内部使用（承德应职院）
- 部署平台：Railway（云端）

---

## 2. 技术栈

| 技术 | 用途 | 版本/说明 |
|------|------|----------|
| **Python 3.13** | 后端语言 | slim 版本部署 |
| **Flask** | Web 框架 | 单文件架构（app.py） |
| **PostgreSQL** | 数据库 | 通过 Railway 部署 |
| **Jinja2** | 模板引擎 | Flask 内置 |
| **Playwright** | 浏览器自动化测试 | channel='chrome' 使用系统 Chrome |
| **Redis** | 缓存（计划中） | 热榜缓存，减少数据库压力 |
| **Docker** | 容器化 | 部署到 Railway |
| **Gunicorn** | WSGI 服务器 | 生产环境运行 Flask |

### 前端技术
- 纯 HTML + CSS + JavaScript（无框架）
- CSS 内嵌在模板中（无独立文件）
- JavaScript 内嵌在模板中
- 所有 AJAX 用原生 fetch API（无 axios）
- 移动端优先的响应式设计

---

## 3. 数据库设计

### 3.1 表结构

#### messages（帖子/评论表）
```sql
CREATE TABLE messages (
    id              SERIAL PRIMARY KEY,
    username        TEXT NOT NULL,              -- 显示名（邮箱@前面部分）
    content         TEXT NOT NULL,              -- 文本内容
    reply_to        INTEGER DEFAULT NULL,       -- 回复的目标帖子ID（NULL=顶级帖子）
    user_email      TEXT DEFAULT NULL,          -- 发布者邮箱
    image_url       TEXT DEFAULT NULL,          -- 图片URL
    category        TEXT DEFAULT 'message',     -- 分类：message/daily/used
    likes_count     INTEGER DEFAULT 0,          -- 点赞数（冗余字段）
    comments_count  INTEGER DEFAULT 0,          -- 评论数（冗余字段）
    created_at      TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

#### users（用户表）
```sql
CREATE TABLE users (
    id          SERIAL PRIMARY KEY,
    email       TEXT NOT NULL UNIQUE,
    password    TEXT NOT NULL,
    role        TEXT DEFAULT 'user',           -- user / admin
    nickname    TEXT DEFAULT NULL,
    avatar_url  TEXT DEFAULT NULL,
    phone       TEXT DEFAULT NULL,
    created_at  TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

#### likes（点赞表）
```sql
CREATE TABLE likes (
    id          SERIAL PRIMARY KEY,
    user_email  TEXT NOT NULL,
    message_id  INTEGER NOT NULL REFERENCES messages(id),
    created_at  TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(user_email, message_id)            -- 一人只能点一次赞
);
```

#### favorites（收藏表）
```sql
CREATE TABLE favorites (
    id          SERIAL PRIMARY KEY,
    user_email  TEXT NOT NULL,
    message_id  INTEGER NOT NULL REFERENCES messages(id),
    created_at  TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(user_email, message_id)
);
```

#### user_config（用户配置表）
```sql
CREATE TABLE user_config (
    email           TEXT PRIMARY KEY,
    api_key         TEXT DEFAULT NULL,          -- DeepSeek API Key
    image_api_key   TEXT DEFAULT NULL           -- 图片生成 API Key
);
```

#### chat_messages（AI对话历史）
```sql
CREATE TABLE chat_messages (
    id          SERIAL PRIMARY KEY,
    user_email  TEXT NOT NULL,
    role        TEXT NOT NULL,                 -- user / assistant
    content     TEXT NOT NULL,
    reasoning   TEXT DEFAULT NULL,
    model       TEXT NOT NULL,
    created_at  TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### 3.2 兼容旧数据库

项目经历过多次数据库迁移，在 `init_db()` 中自动检测并补全缺失的列：

```python
compat_checks = {
    "messages": ["image_url", "category", "likes_count", "comments_count"],
    "users": ["nickname", "avatar_url", "phone"],
    "user_config": ["image_api_key"],
}
# 自动检测缺少的列并 ALTER TABLE ADD COLUMN
```

### 3.3 分类系统

| category 值 | 中文名 | Tab 标签 |
|-------------|--------|----------|
| `message` | 留言板 | 📋 留言板 |
| `daily` | 日常投稿 | 📸 日常投稿 |
| `used` | 二手闲置 | 🏷️ 二手闲置 |

- `reply_to IS NULL` 的为顶级帖子
- `reply_to = 帖子ID` 的为评论
- 评论自动继承父帖子的分类（通过 JS 传当前分类）

---

## 4. 页面结构与路由

### 4.1 页面列表

| 路由 | 模板 | 功能 | 说明 |
|------|------|------|------|
| `/` | `index.html` | 首页（微墙主界面） | Tab切换、帖子列表、底部导航 |
| `/post/<id>` | `post_detail.html` | 帖子详情+全部评论 | 独立评论页，AJAX回复 |
| `/login` | `login.html` | 登录 | |
| `/register` | `register.html` | 注册 | |
| `/ai` | `ai.html` | AI 对话 | 保留功能 |
| `/image-gen` | `image_gen.html` | 图片生成 | 保留功能 |
| `/qrcode` | `qrcode.html` | 二维码 | 保留功能 |
| `/my` | `my.html` | 个人中心（功能入口网格） | 无tab参数时显示8个功能卡片 |
| `/my?tab=posts` | `my.html` | 我的帖子列表 | 带tab参数时显示帖子列表 |
| `/my?tab=favorites` | `my.html` | 我的收藏列表 | 同上 |
| `/my/comments` | `comments.html` | 历史评论 | 查看所有评论+原帖 |
| `/hot` | `hot.html` | 今日热榜独立页 | 🥇🥈🥉 排名展示 |

### 4.2 首页布局（从上到下）

```
┌──────────────────────────────┐
│        承德应职院微墙          │  渐变紫色背景
├──────────────────────────────┤
│  🔍 搜索消息...               │  → 打开搜索弹窗
├──────────────────────────────┤
│  著作者:谢先生  微信号:可复制  │  版权信息
│  🤖 AI助手  🎨 绘图           │  功能入口
├──────────────────────────────┤
│  🔥 今日热榜 [点击展开]        │  折叠面板
├──────────────────────────────┤
│  👤 用户邮箱 | 退出登录        │  用户栏
├──────────────────────────────┤
│  最新 │ 留言板 │ 日常投稿 │ 二手闲置 │  Tab 切换（sticky）
├──────────────────────────────┤
│                               │
│  ┌────┐  用户名               │
│  │头像│  3小时前 · 留言板      │   帖子卡片
│  └────┘                       │
│  今天天气真不错！               │
│  ❤️ 5  💬 3  🗑️              │   操作栏
│                               │
│  ┌────┐  用户名               │
│  │头像│  1小时前 · 二手闲置    │
│  └────┘                       │
│  出自行车...                   │
│  ❤️ 2  💬 1                   │
│                               │
├──────────────────────────────┤
│  🏠 首页     ＋发布     👤 我的 │  底部固定导航
└──────────────────────────────┘
```

### 4.3 底部导航说明

```
🏠 首页      → 回到当前分类页面（刷新）
＋ 发布      → 弹出发布表单（底部弹窗）
👤 我的      → 已登录=回首页，未登录=跳登录页
```

### 4.4 发布弹窗内容

```
┌──────────────────────────────┐
│  📝 发布内容              ✕   │
├──────────────────────────────┤
│  📋 留言板  📸 日常投稿  🏷️ 二手闲置  │  ← 分类选择 radio
├──────────────────────────────┤
│  😊 🖼️                        │  ← 表情/图片工具栏
├──────────────────────────────┤
│  [文本输入框]                  │  说点什么...
├──────────────────────────────┤
│        [发布] 按钮             │
└──────────────────────────────┘
```

---

## 5. 页面跳转流程

### 5.1 核心跳转图

```
首页 ──→ 点搜索框 ──→ 搜索弹窗（热榜TOP10）
                      ├── 输入关键词 → 搜索帖子列表
                      │                └── 点帖子 → 详情页
                      │                            └── ← 返回搜索（保留关键词和位置）
                      └── 点热榜帖子 → 详情页
                                      └── ← 返回搜索
                    
首页 ──→ 点 💬 N ──→ 详情页（全部评论）
                      └── ← 返回首页

首页 ──→ 点 ＋ ──→ 发布弹窗
                   └── 选分类 → 填内容 → 发布 → 跳转到对应分类 Tab

首页 ──→ 点 AI助手 → 新页面 /ai
首页 ──→ 点 绘图   → 新页面 /image-gen
首页 ──→ 点 Tab   → 切换分类（页面刷新）
```

### 5.2 搜索状态保存机制

当用户从搜索结果页点进详情页时，通过以下机制保存状态：

1. **点击帖子时**：调用 `saveSearchState()` 将搜索关键词、结果HTML、滚动位置存入 `sessionStorage`
2. **详情页的 ← 按钮**：使用 `javascript:history.back()` 回退（不是跳转），不产生新历史记录
3. **首页加载/bfcache恢复时**：`pageshow` 事件检测 `sessionStorage` 是否有搜索数据，自动恢复搜索弹窗和滚动位置
4. **关闭搜索时**：调用 `clearSearchState()` 清空 `sessionStorage`，避免下次自动弹出

```javascript
// 关键代码流程
function saveSearchState() {
    sessionStorage.setItem("search_query", query);
    sessionStorage.setItem("search_html", resultsHTML);
    sessionStorage.setItem("search_scroll", scrollTop);
}

function restoreSearchState() {
    // 从 sessionStorage 恢复并返回 true
    // 如果没有缓存数据返回 false
}

// 用 pageshow 代替 load（兼容 bfcache）
window.addEventListener('pageshow', function(e) {
    if (sessionStorage.getItem("search_html")) {
        openSearch();  // 内部先尝试 restoreSearchState()
    }
});
```

### 5.3 搜索流程

```
点搜索框 → 打开弹窗
    ├── sessionStorage 有缓存？ → 恢复搜索结果（保持位置）
    └── 无缓存 → 请求 /api/hot → 显示今日热榜TOP10

输入关键词 → 300ms 防抖 → 请求 /api/search?q=xxx
    ├── 有结果 → 渲染帖子卡片（可点赞、可点进详情）
    └── 无结果 → 显示"未找到相关帖子"

点取消 → 关闭弹窗（不清除缓存，下次打开还保留）
```

### 5.4 点赞流程

```
用户点 ❤️ 按钮
    → 发送 POST /api/like/<id>
    → 服务器 toggle 点赞状态
    → 返回 { liked: true/false, count: N }
    → JS 更新按钮样式（❤️/🤍）和计数
    
特点：无刷新 AJAX，不影响页面其他内容
```

### 5.5 评论流程

```
首页帖子的 💬 N  → 跳转到 /post/<id>
                        ├── 显示完整帖子内容
                        ├── 显示全部评论（时间正序）
                        └── 底部回复框
                            
用户在详情页回复：
    → 输入内容 → 点发送
    → POST /api/reply (JSON格式)
    → 服务器保存评论，返回 { id, username, content, created_at }
    → JS 将新评论追加到评论列表（不刷新页面）
    → 清空输入框
```

### 5.6 第二期导航流

```
🏠 首页 → 底部 👤 → /my（功能入口网格）
                    ├── 📝 我的帖子 → /my?tab=posts（帖子列表）
                    │                   └── 点帖子 → /post/{id}（←返回保留位置）
                    ├── ⭐ 我的收藏 → /my?tab=favorites（同上）
                    ├── 💬 历史评论 → /my/comments（显示评论+原帖引用）
                    │                   └── 点原帖 → /post/{id}
                    ├── 🔥 今日热榜 → /hot（独立热榜页）
                    ├── 📦 我的蹲贴 → toast "功能尚未开发"
                    ├── 💰 跑腿收益 → toast "功能尚未开发"
                    ├── 🛎️ 官方客服 → toast "功能尚未开发"
                    └── 🏫 本校客服 → toast "功能尚未开发"
```

### 5.7 收藏流程

```
用户点 ☆/⭐ 按钮
    → POST /api/favorite/<id>
    → 服务器 toggle 收藏状态
    → JS 更新按钮样式和 Toast
收藏的帖子在"我的→我的收藏"查看
```

### 5.8 头像上传流程

```
用户点头像 → 选择图片
    → POST /api/upload（FormData）
    → 返回图片URL
    → 自动 POST /api/profile/update { avatar_url: "..." }
    → 页面刷新（圆形裁剪显示）
```

**头像同步机制：**
- 帖子列表、详情页、评论的头像都通过 `LEFT JOIN users` 从数据库读取
- 用户换头像后，所有帖子/评论的头像自动更新（下次页面刷新生效）
- 查询语句统一带 `u.avatar_url as author_avatar`

### 5.9 ← 返回按钮规则

```
所有详情页/子页面的 ← 按钮统一用 `javascript:history.back()`：
  - post_detail.html  ✅
  - hot.html          ✅
  - comments.html     ✅
  - my.html           ✅（改掉了 href="/"）
  - ai.html           ✅（改掉了 href="/"）
  - image_gen.html    ✅（改掉了 href="/"）

首页本身没有 ← 按钮，浏览器返回即退出
```

---

## 6. API 接口文档

### 6.1 点赞切换

```
POST /api/like/<int:msg_id>
Auth: 需要登录
Body: 无
Return: JSON { liked: bool, count: int }
说明: 已点赞→取消，未点赞→点赞（toggle）
```

### 6.2 搜索

```
GET /api/search?q=关键词&page=1
Auth: 可选
Return: JSON { messages: [...] }
说明: 全文搜索（ILIKE 模糊匹配 content），返回帖子列表
      每个帖子包含 liked 状态（已登录时）
      每个帖子包含 replies_list（评论列表）
```

### 6.3 今日热榜

```
GET /api/hot
Auth: 可选
Return: JSON { posts: [...] }
说明: 按 (likes_count + comments_count) 降序，取前10条
      已登录时返回 liked 状态
```

### 6.4 AJAX 回复

```
POST /api/reply
Auth: 需要登录
Body: JSON { content: string, reply_to: int }
Return: JSON { id, username, content, created_at }
说明: 不刷新页面，JS 直接追加到评论列表
```

### 6.5 图片上传

```
POST /api/upload
Auth: 需要登录
Body: FormData file
Return: JSON { url: string, filename: string }
限制: 5MB，支持 jpg/png/gif/webp/bmp
说明: 保存到 static/uploads/，返回可访问的 URL
```

### 6.6 提交帖子

```
POST /submit
Auth: 需要登录
Body: FormData { content, category, image_url, reply_to }
Return: 302 Redirect → /?category={category}
说明: 传统表单提交，非 AJAX
```

### 6.7 删除帖子

```
POST /delete/<int:msg_id>
Auth: 需要登录（仅作者和管理员）
Return: 302 Redirect → /
说明: 同时删除关联的 likes 和 favorites
```

### 6.8 收藏切换

```
POST /api/favorite/<int:msg_id>
Auth: 需要登录
Body: 无
Return: JSON { favorited: bool }
说明: 未收藏→收藏，已收藏→取消（toggle）
```

### 6.9 更新个人资料

```
POST /api/profile/update
Auth: 需要登录
Body: JSON { nickname?: string, phone?: string, avatar_url?: string }
Return: JSON { ok: bool }
说明: 只传要修改的字段，不传的不变
```

### 6.10 图片上传

```
POST /api/upload
Auth: 需要登录
Body: FormData file
Return: JSON { url: string, filename: string }
限制: 5MB，支持 jpg/png/gif/webp/bmp
说明: 保存到 static/uploads/，返回可访问的 URL。用于帖子图片和头像上传
```

### 6.11 图片优化说明

上传时自动进行以下优化：
- **格式转换**：统一转 JPEG（PNG 透明背景转 RGB）
- **尺寸限制**：最长边不超过 1200px（按比例缩放）
- **压缩质量**：JPEG quality=80，`optimize=True`
- **缓存头**：返回时加 `Cache-Control: public, max-age=86400`（24小时缓存）

### 6.12 生产环境加速建议

| 方案 | 效果 | 成本 |
|------|------|------|
| Cloudflare CDN | 图片缓存到全球节点，第二次访问秒开 | 免费 |
| 对象存储 OSS | 图片不经过应用服务器 | 有免费额度 |
| Nginx 反代 | 静态文件由 Nginx 直接返回 | 需要自己配服务器 |

推荐先用 Cloudflare（免费，零代码改动），量大后再上 OSS。
```

---

## 7. 自动化测试

### 7.1 测试框架

使用 **Playwright**（微软开发的浏览器自动化工具）。

### 7.2 安装

```bash
pip install playwright
# 不需要安装浏览器！使用系统已安装的 Chrome
# 代码中通过 channel='chrome' 指定
```

### 7.3 测试文件位置

```bash
xiang_mu/project1_message_board/test_browser.py
```

### 7.4 运行测试

```bash
# 确保 Flask 正在运行
cd xiang_mu/project1_message_board
source venv/Scripts/activate
python app.py &

# 运行测试
python test_browser.py
```

### 7.5 Playwright 使用说明（关键！）

```python
from playwright.sync_api import sync_playwright

with sync_playwright() as p:
    # 启动 Chrome（使用系统已安装的 Chrome）
    browser = p.chromium.launch(
        channel='chrome',           # 关键：用系统 Chrome，不必下载新浏览器
        headless=True               # 无头模式（不显示窗口）
    )
    context = browser.new_context(viewport={"width": 1200, "height": 800})
    page = context.new_page()

    # 常用操作
    page.goto('http://localhost:5000')                    # 打开网页
    page.locator('.publish-btn').click()                  # 点击元素
    page.locator('.search-bar input').click()              # 点击搜索框
    page.fill('#publish-content', '你好')                  # 输入文字
    page.locator("label[for='cat-daily']").click()         # 点击 label
    page.locator('input[name="category"][value="daily"]')  # 找 radio
    page.content()                                         # 获取页面 HTML
    page.locator(".post-card").count()                     # 统计元素数
    page.wait_for_timeout(1000)                            # 等待1秒
    page.wait_for_load_state("networkidle")                # 等待页面加载完
    page.set_viewport_size({"width": 375, "height": 812})  # 模拟手机

    # 断言（判断对错）
    assert "承德应职院微墙" in page.content()
    assert page.locator(".btn-del").count() > 0

    browser.close()
```

### 7.6 测试覆盖范围（57项）

| 模块 | 测试内容 |
|------|----------|
| 首页渲染 | 标题、搜索框、热榜、底部导航、Tab栏、4个分类标签 |
| 热榜 | 展开、收起面板 |
| Tab切换 | 3个分类 URL 正确 |
| 登录 | 表单提交、session cookie |
| 发布 | 弹窗显示、分类选择、提交、跳转正确分类 |
| 分类隔离 | 留言板不显示日常帖子，反之亦然 |
| 最新页 | 全部类型混排显示 |
| 点赞 | 点赞/取消、UI反馈 |
| 搜索 | 弹窗、输入、结果、关闭 |
| 回复 | 详情页回复、AJAX追加 |
| "我的"按钮 | 已登录/未登录不同表现 |
| 微信号 | 复制功能 |
| AI/绘图 | 入口存在 |
| 删除 | 删除帖子 |
| 退出 | 退出后状态正确 |
| 移动端 | 375x812尺寸正常显示 |

### 7.7 踩坑记录

#### 坑1：Playwright 浏览器下载失败
- 问题：国内网络无法下载 Chromium
- 解决：用 `channel='chrome'` 指向系统已安装的 Chrome

#### 坑2：Selenium webdriver-manager 连不上
- 问题：ChromeDriver 下载超时/403
- 解决：切换到 Playwright，直接用系统 Chrome

#### 坑3：中文引号导致 Python SyntaxError
- 问题：`"承德应职院微墙"` 中的中文引号与 Python 字符串冲突
- 解决：测试代码中用单引号或转义

#### 坑4：退出登录后仍显示用户名
- 问题：旧帖子中的用户名包含邮箱前缀（3488351542）
- 解决：改为检查"退出按钮消失"而非"用户名消失"

#### 坑5：回复测试找不到元素
- 问题：回复功能从首页 inline 改到了详情页
- 解决：更新测试流程，进入详情页后填回复框

---

## 8. 缓存策略（Redis）

### 8.1 当前状态
当前未使用 Redis 缓存，每次请求都直接查 PostgreSQL。
对于当前数据量（<1000 条帖子）完全够用。

### 8.2 何时需要 Redis
当帖子量达到 **10万+** 或日活达到 **1万+** 时，以下场景适合加缓存：

| 场景 | 缓存策略 |
|------|----------|
| 今日热榜 TOP10 | Redis 缓存 30秒，TTL=30 |
| 分类帖子列表 | Redis 缓存 60秒，TTL=60 |
| 帖子总数/统计 | Redis 缓存 5分钟，TTL=300 |

### 8.3 Redis 实现参考

```python
# 安装
# pip install redis

# 连接
import redis, json
cache = redis.Redis(host='localhost', port=6379, db=0, decode_responses=True)

# 缓存装饰器
def cached(timeout=30):
    def decorator(f):
        def wrapper(*args, **kwargs):
            key = f"{f.__name__}:{str(args)}:{str(kwargs)}"
            result = cache.get(key)
            if result:
                return json.loads(result)
            result = f(*args, **kwargs)
            cache.setex(key, timeout, json.dumps(result, default=str))
            return result
        return wrapper
    return decorator

# 使用
@cached(timeout=30)
def get_hot_posts(email=None, limit=10):
    # 原函数代码不变
    pass

# 缓存失效（帖子变化时）
def clear_hot_cache():
    for key in cache.scan_iter("get_hot_posts:*"):
        cache.delete(key)
```

### 8.4 部署注意
- 本地开发：WSL 或 Windows Redis
- Railway 部署：内置 Redis 插件（一键添加）
- 不要缓存用户相关数据（点赞状态、评论列表）

---

## 9. 开发中遇到的问题

### 9.1 PostgreSQL 相关问题

#### 问题：serial 自增 ID 冲突
- 场景：手动 INSERT 测试数据时指定了 ID，但 sequence 没更新
- 错误：`重复键违反唯一约束"messages_pkey"`
- 解决：
```sql
SELECT setval('messages_id_seq', (SELECT MAX(id) FROM messages));
```

#### 问题：列不存在
- 场景：旧数据库没有新字段（category, likes_count 等）
- 解决：在 init_db() 中检测并 ALTER TABLE

#### 问题：psql 命令找不到
- 场景：Windows PowerShell 中 psql 不在 PATH
- 解决：将 PostgreSQL bin 目录添加到系统环境变量

### 9.2 前端相关问题

#### 问题：发布后帖子显示在错误分类
- 原因：`redirect(request.referrer)` 跳回原来页面
- 修复：改为 `redirect(f"/?category={category}")`

#### 问题：回复后看不到内容
- 原因：回复没有传 category，默认跳到 message Tab
- 修复：AJAX 回复时传当前分类

#### 问题：searchLike 函数未定义
- 原因：搜索结果中的点赞按钮使用了 searchLike 函数
- 修复：在 doSearch 中定义了 searchLike

#### 问题：搜索结果点击后丢失位置
- 原因：页面刷新导致搜索状态丢失
- 修复：使用 sessionStorage 缓存搜索状态和滚动位置

#### 问题："我的"按钮反复登录
- 原因：底部导航写死 /login
- 修复：已登录显示用户名跳首页，未登录才跳登录

### 9.3 Railway 部署相关问题

#### 问题：Gunicorn 超时
- 场景：图片生成 API 耗时 >30秒
- 解决：`--timeout 600`（10分钟）

#### 问题：端口不匹配
- 场景：Railway 设置 8080，但 app 监听 5000
- 解决：Railway Settings → Networking 设置 5000

#### 问题：DATABASE_URL 未注入
- 场景：PostgreSQL 插件的连接串没自动注入
- 解决：Railway Variables → 手动 Refer

### 9.4 测试相关问题

#### 问题：Playwright 浏览器下载失败
- 国内网络无法访问 Google 存储
- 解决：使用 `channel='chrome'` 指向系统 Chrome

#### 问题：Selenium 无法连接
- ChromeDriver 下载网络问题
- 解决：弃用 Selenium，改用 Playwright

---

## 10. 设计决策记录

### 10.1 为什么用 Flask 单文件架构？
- **原因**：项目初期简单，无需复杂目录结构
- **代价**：app.py 现在 800+ 行，可读性下降
- **后续**：如果继续增长，建议拆分为：
  - `app.py`（路由）
  - `models.py`（数据库操作）
  - `utils.py`（工具函数）

### 10.2 为什么不用前端框架？
- React/Vue 需要构建工具，部署复杂
- 纯 HTML 直接部署，Railway 兼容最好
- **代价**：JavaScript 可维护性不如框架

### 10.3 为什么用 AJAX 而不是页面刷新？
- 点赞、搜索、回复如果用页面刷新会丢失滚动位置
- AJAX 提供更流畅的用户体验
- 代价：代码复杂度增加

### 10.4 为什么分类用 text 字段而不是关联表？
- 只有3个固定分类，无需动态管理
- text 字段查询简单，不需要 JOIN
- 未来如果分类多了再考虑用分类表

### 10.5 为什么评论用同一张表？
- 帖子和评论结构完全相同（content, username, image_url）
- reply_to 字段区分帖子和评论
- 简化代码，统一处理
- 代价：查询时需要过滤 reply_to IS NULL

### 10.6 为什么搜索用 ILIKE 而不是 Elasticsearch？
- 当前数据量小（<1000条），ILIKE 足够
- ES 需要额外部署和维护
- 当帖子量 >10万时考虑迁移到 ES

### 10.7 为什么首页不加载评论列表？
- 减少数据库查询次数
- 首页只显示计数，评论在详情页加载
- 提升首页加载速度

### 10.8 为什么 "最新" Tab 不叫 "全部"？
- "最新" 更符合用户直觉
- 按时间倒序排列，展示所有分类
- 分类标签帮助用户区分

---

## 11. 部署指南

### 11.1 本地开发

```bash
# 1. 克隆项目
git clone https://github.com/3488351542/liu_yan_ban.git
cd liu_yan_ban

# 2. 创建虚拟环境
python -m venv venv
source venv/Scripts/activate  # Windows
# source venv/bin/activate    # Linux/Mac

# 3. 安装依赖
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# 4. 确保 PostgreSQL 运行
# 数据库名：message_board
# 用户：postgres
# 密码：123456
# 端口：5432

# 5. 运行
python app.py
# 访问 http://localhost:5000
```

### 11.2 Railway 部署

```bash
# 1. 推送代码到 GitHub
git push origin master

# 2. Railway 自动检测到 Dockerfile，自动构建和部署

# 3. 关键设置
# Target Port → 5000（与 Dockerfile 的 EXPOSE 5000 一致）
# PostgreSQL → 添加插件（自动注入 DATABASE_URL 环境变量）
```

**Railway 特有设置：**

| 设置项 | 值 | 说明 |
|--------|-----|------|
| Target Port | 5000 | 必须和 Dockerfile 一致 |
| Root Directory | 留空 | 项目根目录 |
| Build Command | 检测 Dockerfile | 自动 |
| Start Command | 使用 Dockerfile | CMD 已配置 gunicorn |

**常见错误：**

| 错误 | 原因 | 解决 |
|------|------|------|
| 502 Bad Gateway | Target Port 没设对 | Settings → Networking → Target Port = 5000 |
| Worker timeout | 图片生成 >30秒 | Dockerfile 加 `--timeout 600` |
| DATABASE_URL 不存在 | 没关联 PostgreSQL | Variables → 手动 Refer 数据库服务 |
| 数据库列缺失 | 旧数据库没有新字段 | 重启服务，init_db 自动补全 |

### 11.3 Docker 部署

Dockerfile 已配置：
```dockerfile
FROM python:3.13-slim
WORKDIR /app1
COPY requirements.txt .
RUN pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
COPY . .
EXPOSE 5000
CMD ["gunicorn","app:app","-b","0.0.0.0:5000","--limit-request-field_size","16380","--limit-request-line","16380","--timeout","600"]
```

### 11.4 requirements.txt

```
flask>=3.0
psycopg2-binary>=2.9
werkzeug>=3.0
requests>=2.31
qrcode>=7.4
gunicorn>=22.0
playwright>=1.50
redis>=5.0
```
